# 交辦狀態（監督那條線寫的，2026-09-12 18:06）

這份是給壓縮之後的自己看的。它只記「現在要做什麼」與「為什麼這樣做」，
細節在各自的文件裡。

## 手上這一串（順序不能換）

105 句生成中（b76wdslkm）→ 補發佈 → 105 那一欄歸零 → 清 57 個孤兒 → 掛載（含 voiceMode）→ storylint

判準：storylint 的 S3 要從 SKIP 轉紅（大量缺漏但不是全缺），S4 要從紅轉綠。
兩個都要轉。只轉一個就是有一半沒做到。S4 的 voiceMode 已經先補上了，
所以掛完它還是紅的話，表示那個鍵被掛載流程蓋掉了——那是新問題不是失敗。

規格在 ~/glitch-live/規格-配音與BGM掛回板子.md（那個 repo 唯讀）。
第四節四步的順序不能換，第 6 步 storylint 才是判準，不是第 5 步推送成功。

## 已經定案、不要再動的

選角：管理員／材料行老闆／櫃檯維持本機；保全走 Larch 紳士、店員走 Larch 南方青年。
  依據：保全與店員的本機參考音是女聲（230 Hz、244-246 Hz），角色是男性，輸入本身錯。
  紳士在保全真實長句上量到 0.060／1.19，跟最穩的南方青年 0.037／1.12 同級。

BGM 六槽：store B、parts A、figure B（證據較弱）、laundry A（單支無 A/B）、
  transit B（外推自 store 那輪的原則，證據強度不同，已標明）、latenight 候選不接進遊戲等本人。

通關路線基準：MODE=free 那份已覆蓋並進版控，檔頭帶十個環境變數（沒設的也寫出來）。
  驗收標準是統計一致，因為 board.html:262 有未定種子的 Math.random()。

## 不要再花力氣的

- BGM 任何一槽都不要再多跑一輪代聽。六槽都有結論了。
- 不要再試著用 prompt 產出差異更大的 B。三組獨立證據顯示那個產生器不隨 prompt 細節變化。
- 2053 個語音檔的全長掃描已經做完（桌寵那條跑的），結論是排除不是發現，
  六支要人耳判的已經整理給本人。不要重跑。

## 等本人的

latenight 那一槽要不要、以及六支語音檔要不要重生。其餘都不擋。

## 做到一半失敗長什麼樣（每一條都帶怎麼發現）

**失敗到一半的狀態通常沒有名字，而且看起來跟成功一樣。** 這一節寫的是怎麼分辨。

### 105 句生成只換了一部分

`gen105.py` 直接覆寫 `art/voice/<雜湊>.mp3`，所以中途失敗會留下新舊混雜——
有些是紳士／南方青年，有些還是原本的女聲，**從檔名與大小完全看不出來**。

怎麼發現：

    ~/voice-venv/bin/python tools/check_cast_f0.py

換成功的話，保全的比值要從 **3.75** 掉下來（它現在對不上自己那支女聲參考音是正常的，
因為參考音已經不是他要用的了），店員的產出中位要從 **253 Hz** 掉進男聲區間 75–185。
**沒掉就是有一批沒換到。** 兩個角色要分開看，可能只有一個換成功。

（注意：換完之後 `check_cast_f0.py` 的第一層對這兩個角色會失去意義——
它比的是「產出對參考音」，而他們的參考音已經不再是要用的那支。第二層仍然有效。）

### 掛載只掛了一部分

有些卡有 `voiceUrl` 有些沒有。**那跟「105 句還沒補」長得一模一樣**：storylint 的 S3
兩種情況都顯示部分缺漏。

怎麼分辨：數缺漏的句數，跟 105 對。

    # 線上板子有多少台詞行沒有 voiceUrl
    # 正常（105 句還沒補）：缺的應該剛好是保全 57 ＋ 店員 48
    # 異常（掛載中途失敗）：缺的會散在各個角色

缺的如果不是那兩個角色、或數字不是 105，就是掛載沒做完。

### 補發佈只更新了一部分

`publish_voice.py` **只加不刪**，所以 `urls.json` 部分更新之後看不出哪些沒更新到——
表裡每一筆都有網址，但有些指向舊檔。

怎麼發現：對帳兩件，不要只對一件。

    # 一、表裡每個代號在 docs/voice 都要有檔
    # 二、docs/voice 的檔要跟 art/voice 的**內容**相同（不是大小，見 publish_voice.py 檔頭）

第二件是關鍵：統一響度之後有些檔大小剛好一樣，比大小會整批跳過而線上還是舊的聲音
（那是別人踩過寫下來的）。

### voiceMode 被掛載流程蓋掉

S4 會從綠退回紅。**那是新問題不是失敗**——`languages[].voiceMode` 在掛載之前已經補上，
所以掛完變紅表示掛載的寫入把它洗掉了，要去看掛載是不是整包 PUT 了 project。

怎麼發現：掛載前後各讀一次。

    # 兩次都要是 [{"code":"zh-Hant","label":"繁體中文","voiceMode":"shared"}]

### 任何一次 PUT 之後

**先對卡數再看別的。776 張**（board-main 770 ＋ board-credits 6）。
`PUT /projects` 會清空版子而且不報錯，快照在
`快照/調查篇-2026-09-12-掛載前.json.gz`。

## 19:30 掛載跑完之後補的兩條

**六、掛在其中一個入口上。** 卡片不是只從 `card_node()` 出來的——那支自己就有
四個 return，另外還有錄音反應卡與錄音帶卡是流程裡直接 `b.add()` 組 data 的。
掛載寫在 `card_node()` 裡的時候，275 張單人卡加 7 句錄音相關的卡整批沒有聲音，
而板上看起來跟有聲音的卡一模一樣。收口的位置是 `Board.add()`。

    怎麼查：建置印的「板上 N 句」要對得上 storylint 的「台詞 N 句」。
    1403 對 1828 的差額必須逐項說得出來（舞台指示、路人、沒有講者的卡），
    說不出來的那一截就是漏掉的入口。

**七、換角換了一半，而且是往「有聲音」的方向壞。** 保全與店員改走 Larch 之後，
`urls.json` 裡改判之前生的 121 句還在，`novelkit._voice()` 照樣掛得上去。
板上於是有 121 句「有聲音、而且是女聲配男角」。**這比沒有聲音更難發現**，
因為所有「掛了幾句」的數字都會變好看。

    怎麼查：`speaker in LARCH_VOICE` 而且 `voiceUrl` 有值的行，必須是 0。
    修法是掛的時候就把那些拔掉（build.py 的 _attach_voice），不是等聽出來。
