"""配音的對應表與工具。

**播放器有 voiceUrl，卡片層與每一行對話都有**（行的優先，沒有才用卡片的），
所以我們可以自己生音檔上傳，不必用平台那 280 次的即時生成額度。
而且自己生才控制得住聲線——同一個角色不可以有兩種聲音。

檔名用「講者＋台詞」的內容雜湊：卡片位置變動不用重生，只有改字才重生那一句。
"""
import hashlib

# 每個角色的參考音（zero-shot clone 要「一段真人講話 + 那段話的逐字稿」）。
# 選角素材在 ~/下載/cosyvoice-test/jinn-casting3（42 位台灣人，各有原聲與 clone 成品）。
# 值填 (參考音路徑, 逐字稿路徑)；還沒選定的先留 None。
REF = "/home/ct/下載/cosyvoice-test/jinn-casting3"

# **走外部配音的角色。** gen_voice 看到缺檔就會用本機補，它不知道這個角色的
# 聲音是別處來的——切割漏掉幾句，下一次跑就會靜默地把那幾句換成另一個人。
# 實際發生過：黑洞先生有五句被本機補成 92～99 Hz 的聲音，跟他自己差很多。
# 這幾個角色缺檔就是缺檔，要回頭補切，不可以就地生。
EXTERNAL = {"黑洞先生", "諾亞", "鐵塔"}

# 值是 (參考音, 那段話的逐字稿, 語速)。降/升半音的參考音已經一起放進 REF，
# **不要指向 scratchpad**，那個目錄會隨 session 消失，聲線就重建不出來了。
VOICE = {
    "0x":      (f"{REF}/13-原聲.wav", "喔喔原來是政府的新聞稿才行", 1.0),
    # 諾亞也走外部配音（MiniMax「詼諧長者」）。他的核心設定就是七十幾歲，
    # 用真的老人音色比把成年男聲降三個半音準。成品已在 art/voice/。
    "諾亞":     (f"{REF}/04-原聲.wav", "右邊是三峽溪與拱橋", 0.9),
    # **鐵塔已經改用外部配音**（Larch 平台的 MiniMax「廣播主持」音色，
    # 整份十八句一個檔，用 tools/split_take.py 切開後放進 art/voice/）。
    # 成品已經在那裡，gen_voice 會自動跳過他，這一列留著只是備援：
    # 要重生的話它跟諾亞同一支聲源差三個半音，全書兩人沒有同時開口。
    "鐵塔":     (f"{REF}/04-dn3.wav", "右邊是三峽溪與拱橋", 1.0),
    "旁白":     (f"{REF}/19-原聲.wav", "請由下列兩個題目中任選一題", 1.15),
    # 黑洞先生也走外部配音（MiniMax「紳士」，97Hz——全場最低）。成品已在
    # art/voice/，這一列是備援。
    "黑洞先生":  (f"{REF}/33-原聲.wav", "咖啡好喝，點心也很好吃", 0.95),
    "斑比":     (f"{REF}/34-原聲.wav", "所以現在社遊照辦地點未定", 1.0),
    # 格莉奇的聲線是兩段接力做出來的：
    # 一、聲源來自《格莉奇 4KB》那首歌——副歌前那段口白是**唸的不是唱的**，
    #    用 librosa 的 REPET-SIM 去掉伴奏，照 whisper 抓到的停頓切在句子邊界（7.85 秒）。
    #    **參考音不可以超過十秒**：十五秒那版會把 prompt 的內容漏進輸出，
    #    生出來的句子前面會多一句「我可能會忘記你」。
    # 二、再拿那支生出來的三句乾淨說話，接成 7.1 秒當**新的**參考音（就是這支）。
    #    複製的複製反而更好用：歌聲來源會帶旋律，自 clone 從源頭就沒有，
    #    而且短句（「欸。」「喔。」）用歌那支會糊掉，用這支不會。
    "格莉奇":   ("voice-ref/glitch.wav",
               "有五百次。這首是我知道的那一首。一、二、三、四、五、六。", 1.0),
    # 想讓她哼起來的段落改用這支，旋律感是從歌裡帶出來的。
    "格莉奇-唱": ("voice-ref/glitch-song.wav",
               "雖然我的記憶體只有四KB，但我會把你們給我的每一個支持，", 1.0),
    # 貓草二十三歲。**不要用 MiniMax 那支**：它給他 114Hz，跟諾亞的 112 幾乎
    # 一樣，兩個人會聽成同一個。27 號是 199Hz，年紀才對得上。
    "貓草":     (f"{REF}/27-原聲.wav", "於上班時間親洽辦公室", 1.0),
    # 兩年前直播畫面裡的格莉奇。同一個人，所以同一支聲源，只把語速拉快一點——
    # 那時候她講話比較用力。**不可以另外選角**，換聲音就變成另一個人了。
    "畫面裡的她": ("voice-ref/glitch.wav",
               "有五百次。這首是我知道的那一首。一、二、三、四、五、六。", 1.06),
    # 第六章路上的大學生，只有兩句。33 號本來是留給黑洞的，他改走外部配音之後
    # 就空出來了，拿來配路人剛好不會跟任何主要角色撞。
    "大學生":    (f"{REF}/33-原聲.wav", "咖啡好喝，點心也很好吃", 1.0),
}


def key(speaker, text, emotion=None):
    """一句話的穩定代號。

    情緒也算進去——同一句話用不同語氣唸是兩個音檔，不可以共用。
    """
    h = hashlib.sha1(f"{speaker}|{text}|{emotion or ''}".encode("utf-8")).hexdigest()[:16]
    return f"v-{h}"


# ── 表演指示 ────────────────────────────────────────────────────────────
# **zero-shot 只複製音色，也複製參考音的語氣。** 選角素材是 Common Voice 那種
# 照稿唸的錄音，所以複製出來的每一句都一樣平。要有情緒得走 inference_instruct2，
# 它多吃一段「表演指示」。
#
# 指示的格式是死的：`You are a helpful assistant. <指示><|endofprompt|>`。
# 少了 <|endofprompt|> 會先丟 AssertionError，然後炸在卷積層，錯訊寫的是
# 「Kernel size can't be greater than actual input size」，完全看不出跟指示有關。
# 包在 tools/voice_batch.py 裡了，這裡只寫指示本身。

# 每個角色的底色。沒有標 emotion 的句子就用這個。
BASE = {
    # **不要加「放柔」。** 覺得她兇的時候試過四種指示（現在／放柔／溫柔／軟），
    # 五句盲聽下來四句挑到「現在」這一版——改指示反而不如原本。
    # 兇是每次生成擲骰子的結果，不是設定造成的：同一組指示既生得出兇的
    # 也生得出好的。也試過用音高篩選重做，那更糟（三十八句平均升高 14 Hz）。
    # 真正有效的做法是聽到哪一句不對就指名，那一句生幾版挑一版。
    "格莉奇": "用平常講話的語氣，不要唱",
    "黑洞先生": "用低沉、平淡、幾乎不帶起伏的語氣說，話很少的人在講話",
    "0x": "用壓抑的、幾乎沒有起伏的冷淡語氣說，字字清楚",
    "諾亞": "用一位七十幾歲老人溫和、緩慢、寬容的語氣說",
    "鐵塔": "用公事公辦、俐落簡潔、沒有多餘情緒的語氣說",
    "斑比": "用有點猶豫、聲音偏小的語氣說",
    "旁白": "用平穩、清楚、不帶個人情緒的旁白語氣說",
    "貓草": "用年輕、隨便、有點吊兒郎當的語氣說",
    "畫面裡的她": "不要唱，比平常更亮、更用力，像在鏡頭前",
    "大學生": "用疲憊、沒什麼耐性的語氣說",
}

# 卡片上的 emotion 直接當表演指示用——那些值本來就是為了選表情差分而標的，
# 標的就是「這一刻他是什麼狀態」，跟配音要的是同一件事，不必再標一次。
# 這裡只列「講出來跟臉上不一樣」或差分名稱不成句的，其餘 fallback 回 BASE。
EMO = {
    ("格莉奇", "開心"): "不要唱，很開心、語速偏快",
    ("格莉奇", "難過"): "不要唱，聲音壓低、有一點哽住",
    ("格莉奇", "笑"): "不要唱，一邊笑一邊講",
    ("格莉奇", "當機"): "不要唱，遲疑、斷斷續續，像想不起來",
    ("格莉奇", "發呆"): "不要唱，慢、飄，像在想別的事",
    ("格莉奇", "驚訝"): "不要唱，突然拔高、很意外",
    ("格莉奇", "在算"): "不要唱，一邊算一邊講，有停頓",
    # 第七章 0x 說「我說不要」之後那句「……喔。」。**不是吃驚，是洩氣**：
    # 她這一刻才知道當年被拒絕過。前面那個刪節號要真的停一下才有那個意思。
    # **她在家跟黑洞先生講話，跟她在直播上是兩種語氣。** 全部走同一個底色的話，
    # 深夜客廳那幾句會用直播的力道講出來，聽起來像在吵架——第一章那一段是
    # 全書的情感核心（「你知道那是多少嗎？」之後他才說「比七多」），
    # 第七章客廳更是兩年的事講開的那一夜，二十八句都在那裡。
    ("格莉奇", "深夜家常"): "不要唱，語氣疲倦，聲音放低",
    ("格莉奇", "洩氣"): "不要唱，聲音很小、洩了氣，尾音拖長慢慢淡掉，像剛知道一件不太好的事",
    ("黑洞先生", "難得的溫柔"): "用低沉的語氣說，比平常軟一點，很輕",
    ("0x", "意外"): "用冷淡的語氣說，但有一瞬間被打斷的遲疑",
    ("0x", "要走"): "用冷淡、收尾的語氣說，像已經站起來了",
    ("0x", "完全的平"): "用完全沒有起伏的語氣說，像在唸一份文件",
    ("斑比", "不安"): "用有點猶豫、聲音偏小的語氣說，尾音不穩",
    ("斑比", "被說中"): "用被說中心事的語氣說，先停一下再講",
    ("斑比", "累"): "用很累的語氣說，聲音散掉",
    ("斑比", "專注"): "用專注、平穩、講重點的語氣說",
    ("諾亞", "笑"): "用一位七十幾歲老人的聲音說，帶著笑意",
    ("諾亞", "和藹"): "用一位七十幾歲老人溫和、寬容的語氣說",
    ("諾亞", "想事情"): "用一位七十幾歲老人的聲音說，慢，中間有想事情的停頓",
    # 第六章那句「我要記住每一個來的人。我保證。」。她的底色是「更亮、更用力」，
    # 那是兩年前直播的常態，但**這一句要認真**——她真的以為她做得到，
    # 認真才會在第七章痛。前一句旁白剛寫過她笑得很用力，對比就在這裡。
    # **指示要短，而且不可以引用台詞的字。** 模型會把指示唸出來，兩種情況都中過：
    # 一次是寫「最後那句『我保證』放慢」（引用了台詞），一次是寫
    # 「一邊笑一邊講，像在跟很熟的朋友互相虧來虧去，不是真的生氣」——
    # 那條沒有引用任何台詞，純粹是太長（三十七個字），整條被唸了出來。
    # 二十字以內比較安全。「唸紙上的字」原本三十六字，後來真的被唸出來了。
    # 還有兩條超過三十字（認真／洩氣），那兩句人聽過認可、目前沒洩漏，
    # 所以留著——但**那幾句如果重生，要特別聽有沒有把指示唸出來**。
    ("畫面裡的她", "認真"): "不要唱，用一個小女孩的聲音對著鏡頭認真地說，"
                        "不要亢奮，語速放慢，收尾講得很篤定",
    # 旁白唸本子上的字。**那是她自己的筆跡，不是有人在下命令**，
    # 當成命令唸就會很兇。要像照著紙唸：輕、平、慢，沒有情緒。
    # 原本這條三十六字，聽起來內容完全不對——**指示被唸進去了**。
    # 縮到二十字以內就好，見上面那條規則。
    ("旁白", "唸紙上的字"): "不要唱，很輕地照著唸，沒有情緒",
    ("鐵塔", "疲憊"): "用公事公辦的語氣說，但聽得出來很累",
    ("鐵塔", "難得的溫柔"): "用公事公辦的語氣說，最後一句軟下來",
}


# instruct2 的訓練資料以大陸普通話為主，不寫腔調就會被拉過去，而且**同一個指示
# 連生幾次會愈漂愈遠**。方言指示是吃的（官方範例就是「用四川話說這句話」），
# 所以每一句都掛上，不是只補某個角色——674 句要一致。
ACCENT = "用台灣腔的中文說"


def instruct(speaker, emotion=None):
    """一句台詞要用的表演指示。找不到情緒就退回角色底色。"""
    base = None
    if emotion:
        base = EMO.get((speaker, emotion))
    if base is None:
        base = BASE.get(speaker)
    if base is None:
        return None
    return f"{ACCENT}，{base}"


# ── 讀音替身 ────────────────────────────────────────────────────────────
# **畫面上顯示的字，跟送進 TTS 的字，不必是同一組。** 小說正文一個字都不用改，
# 只換餵給模型的那一份。破音字、外來語、還有唸起來不對勁的詞都走這裡。
#
# 「少來」是實例：它不是唸錯，是那兩個字會把整句的音域拉低，格莉奇聽起來變成
# 小男生。換成「哎唷，少來」也一樣，所以不是句首的問題，是那個詞本身。
SUB = {
    # 小說寫成「這是假的。」她說。「周邊。」——兩段引文中間夾著敘述。
    # VN 把它併成一張卡，唸出來中間那個句號變成一個停頓，聽起來卡卡的。
    # 畫面上照舊，唸的時候接起來。
    "這是假的。周邊。": "這是假的周邊。",
    # 「啞」會被唸成別的音（聽起來像「沙子」）。「雅」同音，畫面上照舊寫沙啞。
    # 「啞」模型唸不掉，兩處都中過（「沙啞」聽成沙子、「會啞」聽成會的）。
    # 「雅」同音，畫面上照舊。
    "沙啞": "沙雅",
    "會啞": "會雅",
    # 「轉」在這裡是轉發的轉（三聲），模型唸成四聲。補成「轉發」這個完整的詞
    # 就唸對了，畫面上照舊寫「轉的」。
    "斑比自己轉的": "斑比自己轉發的",
    # 「行」在這本書裡幾乎都是 háng（本子上的一行），而**只有斑比那一句唸錯**，
    # 其他四十幾句都對。所以不做全域替換：「航」跟「行」同音，全域換不會弄壞
    # 已經對的那些，但會害五十二句白白重生一次，而每一次重生都是重新擲骰子。
    "這裡為什麼是七行。": "這裡為什麼是七航。",
    # 洩氣是尾巴拖出來的，不是開頭停出來的。畫面上照舊寫「……喔。」。
    "……喔。": "喔………",
    # 小數點唸不對，要寫成國字。「99.97」會被唸成別的東西，
    # 「九九點九七」才對。畫面上顯示的還是數字，小說正文一個字都不用改。
    # **百分比要排在前面。** to_speech 是照順序取代的，先換掉「99.98」的話
    # 就會留下一個孤兒百分號，唸成「九十九點九八%」。
    "99.98%": "百分之九十九點九八",
    "99.96%": "百分之九十九點九六",
    "99.94%": "百分之九十九點九四",
    # 唸「九十九點…」不是「九九點…」——這是一個分數，中文的自然唸法是前者。
    # 逐字唸的版本聲調會歪（九九連著兩個三聲，模型沒有做變調）。
    "99.98": "九十九點九八",
    "99.97": "九十九點九七",
    "99.96": "九十九點九六",
    "99.94": "九十九點九四",
    "0.01": "零點零一",
    "0.04": "零點零四",
}


def to_speech(text):
    """把要顯示的文字換成要唸的文字。"""
    for a, b in SUB.items():
        text = text.replace(a, b)
    return text


# ── 長句不要自己切 ──────────────────────────────────────────────────────
# 試過先切成四十字以內再接起來，**比整段丟進去差**：每一段是各自合成的，
# 指示會重新擲一次骰子，所以段與段之間的情緒對不起來，聽得出來在飄。
# CosyVoice 內部本來就會分段（inference_instruct2 → text_normalize(split=True)，
# 在 60～80 個 token 切），voice_batch 再把它吐的段落接起來，那條路是穩的。
# 標點也不會被唸出來，它變成停頓。所以這裡什麼都不做。
