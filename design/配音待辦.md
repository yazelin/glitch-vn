# 配音待辦

## 等外部重生（MiniMax）

目前沒有。黑洞先生的「嗯。」已經用新錄音換掉（不再是問句），
「而我每天都在這個房間裡。」也補上了，五十句全到齊。

## 等重生（本機）

目前沒有。0x「調回來的那個會先停頓一下再回答。」已換成她的底色語氣
（原本聽起來像在唸一份文件），代號 `v-c8bf530d77691a3e`。

## 要查

目前沒有。

- ~~諾亞「又鎖起來了喔。」聽起來沒聲音~~（第五章他的第一句）：2026-08-31
  使用者回報聽得到。檔案本身一直是好的（1.05 秒、-18.3 dB），所以原本那次
  多半是瀏覽器拿到快取的舊檔。**下次遇到「某一句沒聲音」先強制重新整理
  （Ctrl+Shift+R）再開單。**
- ~~**嚨**（清喉嚨，第二章）~~：2026-08-31 解掉了，替身是「喉龍」，
  小說原文一個字都沒動。定案方法見 `design/讀音替身表.md`：同一句各生兩版、
  送 whisper 比拼音，喉龍與猴龍兩版都聽回「喉嚨」，喉隆只對一版、後龍兩版都錯。

## 已解決的坑（本節之外）

- **鐵塔的參考音一直指錯（2026-08-31 修）。** `voice.VOICE["鐵塔"]` 指著
  「諾亞降三個半音」（`04-dn3.wav`），可是他自己的克隆參考音
  `voice-ref/tower.wav` 早就在 repo 裡。實聽判定降半音那版不像他——
  **差三個半音只對得上音高，對不上音色**。已改指他自己那支。

## 已知但暫時不動

- **黑洞先生第七章那五句的「行」。** 卡 48／50／58／59／64，「行」要唸 háng。
  「航」這個替身是為本機的 CosyVoice 加的，MiniMax 不見得有同一個毛病，
  聽過判定沒問題，不重生。真的要補的話台詞在
  `design/台詞/黑洞先生-第七行五句.txt`。

- **傘 → 閃、飲水機 → 淫水機、鑰匙 → 要匙。** 三個都是模型唸不出來的字，
  改用同音字替身解決，畫面上的字一個都沒有動（見 `design/讀音替身表.md`）。
- **「嗯。」三次共用同一個音檔。** 同樣的講者、字、情緒就是同一個代號，
  所以第二章那三處是同一支。真人配音也常這樣，但連著聽會像跳針。
  要分開的話得給那幾處各標不同的情緒。
- **貓草「…………」** 只有標點，沒有可唸的內容，永遠不會有配音。
- **留言區那一句** 是螢幕上的留言文字，不是有人在講話。

## 已解決的坑（別再踩）

- 指示要短，二十字以內。太長會被模型整條唸出來（三十七字、三十六字都中過）。
- 指示裡不可以引用台詞的字，同樣會被唸出來。
- 「啞」「行」「轉」模型會唸錯，用同音字替身，畫面上的字不動（見 `voice.SUB`）。
- 外部配音的角色缺檔不可以用本機補，會變成另一個人的聲音（見 `voice.EXTERNAL`）。
- 系統訊息（「貓草已離線。」）不唸，見 `novelkit.chat` 的 `speakText`。
