# 配音待辦

## 等外部重生（MiniMax）

目前沒有。黑洞先生的「嗯。」已經用新錄音換掉（不再是問句），
「而我每天都在這個房間裡。」也補上了，五十句全到齊。

## 等重生（本機）

目前沒有。0x「調回來的那個會先停頓一下再回答。」已換成她的底色語氣
（原本聽起來像在唸一份文件），代號 `v-c8bf530d77691a3e`。

## 要查

- **諾亞「又鎖起來了喔。」聽起來沒聲音**（第五章他的第一句）。
  檔案本身是好的：1.05 秒、-18.3 dB，跟其他句子一樣。所以問題在別處——
  可能是轉場時就開始播被蓋掉，或者瀏覽器拿到快取的舊檔。
  驗的時候記得強制重新整理（Ctrl+Shift+R）。

### 模型唸不出來、而且換寫法也救不了的兩個字

這兩個要嘛接受它唸錯，要嘛改小說原文的用詞。試過的寫法都列在下面。

- **飲**（飲水機，第三章休息室三句）：開飲機／飲水器／飲水機器／加逗號／三種語速
  → 蒙水機、冷水機、永水機、門水機、開泳機。只有「那台機器」唸得對，
  但那失去了具體感——那台飲水機一秒一滴，是那一幕唯一的聲音，0x 一直看著它。

- **鑰**（鑰匙，第五章）：鑰匙／鎖匙／那把鑰匙／那把鑰匙(換句)
  → 測試、瑣士、劍士、鑽石。**改句子才有解**：
  「門鎖是電子的，她根本不用開。」唸得對。

## 已知但暫時不動

- **傘 → 閃。** 唸成「三、語3、塞」，改用同音字替身解決，畫面上的字不動。
- **「嗯。」三次共用同一個音檔。** 同樣的講者、字、情緒就是同一個代號，
  所以第二章那三處是同一支。真人配音也常這樣，但連著聽會像跳針。
  要分開的話得給那幾處各標不同的情緒。
- **貓草「…………」** 只有標點，沒有可唸的內容，永遠不會有配音。
- **留言區那一句** 是螢幕上的留言文字，不是有人在講話。

## 已解決的坑（別再踩）

- 指示要短，二十字以內。太長會被模型整條唸出來（三十七字、三十六字都中過）。
- 指示裡不可以引用台詞的字，同樣會被唸出來。
- 「啞」「行」「轉」模型會唸錯，用同音字替身，畫面上的字不動（見 `voice.SUB`）。
- 外部配音的角色缺檔不可以用本機補，會變成另一個人的聲音（見 `voice.EXTERNAL`）。
- 系統訊息（「貓草已離線。」）不唸，見 `novelkit.chat` 的 `speakText`。
