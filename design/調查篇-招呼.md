# 《調查篇》進門那一下（2026-09-07）

她一直去，就會有人記得她。這一層是每個常駐的人在她進門那一下的反應，照碰過幾次換：
第一次沒有反應，第三次起有一個小動作，第五次起先開口。
**櫃檯是唯一的例外**：她那一句永遠一字不差，換的是她背後的東西，理由見那一節或先做。**只演動作，旁白不說「他記得你了」**
（`調查篇.md` 零：那些重複是溫柔的，點破就變說教）。

建置：`larch/inv/build.py` 認這一份，接在地點入口與選單之間，照 `met_<誰>` 選一段，沒有對到就直接進選單。
每一段一到兩張卡，不寫任何變數，不給任何線索。時段跟著那個人在的時段（`調查篇-場景.md` 二）。

櫃檯是唯一的例外：她的招呼每一次都一模一樣，而且要用同一份音檔。她是唯一不磨損的人，也是唯一不記人的人。

---

## 管理員（`lobby`・上午／下午）

### 第三次起（`met_管理員 >= 3`）

**旁白**
> 窗口開著。他抬頭看了一眼，沒有問找誰。

### 第五次起（`met_管理員 >= 5`）

**旁白**
> 她還沒走到窗口，他已經把報表翻到下一頁，往旁邊挪了一格，讓出窗台的位置。

## 諾亞（`roof`・上午／下午／晚上）

### 第三次起（`met_諾亞 >= 3`）

**talk**（諾亞／玩家）
> *他沒有抬頭，手上的東西沒停。*
>
> **諾亞**：門帶一下。

### 第五次起（`met_諾亞 >= 5`）

**旁白**
> 工作檯旁邊多了一張椅子，面向他。他沒有說那是給誰的。

## 店員（`store`・任一時段）

### 第四次起（`met_店員 >= 4`）

（第三次的晚上是第一天定稿那一場「喔，是你」，招呼從第四次起才用這一張，不然同一趟會先「沒說歡迎光臨」再「歡迎光……」。2026-09-08 改。）

**旁白**
> 他看了一眼，點了一下頭。沒有說歡迎光臨。

### 第五次起（`met_店員 >= 5`）

**旁白**
> 中杯熱美式已經放在櫃台上。他還在補貨，沒有回頭。

## 材料行老闆（`parts`・上午／下午）

### 第三次起（`met_材料行老闆 >= 3`）

**talk**（材料行老闆／玩家）
> *她沒有抬頭，把老花眼鏡推上去。*
>
> **材料行老闆**：還沒到。

### 第五次起（`met_材料行老闆 >= 5`）

**旁白**
> 櫃檯上放著一張紙條，寫著到貨的日期。她指了指，繼續包東西。

## 櫃檯（`tower14`・上午／下午）

### 第三次起（`met_櫃檯 >= 3`）

**旁白**
> 她的手在預約簿上停了一下，翻到下一頁，然後抬頭。

### 第五次起（`met_櫃檯 >= 5`）

**她那一句永遠一字不差**，而且那一句在「跟櫃檯約訪問」那一格裡，不在這裡。
這一條是 0x 那一場的支點：0x 說「你來過六次」，而櫃檯不記得。
要是讓櫃檯開始認得她，那個對比就沒有了。所以這兩級換的是她背後的東西，
玩家看得出時間在走，可是她對玩家的反應一次都沒有變（2026-09-10）。

**旁白**
> 桌上那盆花換過了。同一個花瓶，這次是白色的。
>
> 她抬頭的時間跟前幾次一樣長。

## 保全（`tower14`・晚上／深夜）

### 第三次起（`met_保全 >= 3`）

**旁白**
> 他看了她一眼，沒有說這裡不能站。

### 第五次起（`met_保全 >= 5`）

**旁白**
> 桌邊多了一張摺疊椅。他沒有看她，把對講機的音量轉小了一格。

## 斑比（`studio`・下午／晚上／深夜）

### 第三次起（`met_斑比 >= 3`）

**旁白**
> 椅子上那些東西已經被推到一邊了。她沒有回頭。

### 第五次起（`met_斑比 >= 5`）

**旁白**
> 七個馬克杯裡，有一個放到了靠門這一邊。是空的。
