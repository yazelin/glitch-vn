# 接手筆記（2026-09-12）

這一份是給壓縮之後的自己、或別的 session 接手用的。**只寫還沒落進其他文件的東西**，
機制與規格看 `README.md`、`design/調查篇.md`、`larch/RELEASES.md`。

## 一、正篇《格莉奇與黑洞先生》

`project-bec1644c-0dfe-4447-86c0-0c592e2f939f`。**這一輪的東西都做完了，等 yazelin 發市集。**

- CG 收藏十四張改成走到那一場才解開（原本開場就全看得見，等於劇透）
- 「聯動・猜歌」收的圖換成第三章真正用的 `bg-collab-studio`
- 白板七章重排成蛇行網格（**只動 position**，卡片 id 沒變，讀者存檔不受影響）
- 第四章一處立繪：黑洞先生從 `farRight` 移到 `left`，原本跟格莉奇疊在同一格
- 拿掉專案上殘留的日語設定（只有第一章被翻過 81 張，其餘六章沒有）

驗過：`larch/verify.py` 全綠、Playwright 從標題玩到謝幕 1118 張卡沒卡住、
十四張 CG 各在該解的地方解開、走到一半只解七張時另外七格仍是鎖的。

**發佈是 yazelin 自己按。** `POST /publish` 填不進更新說明，要在網頁後台第 ⑤ 區貼，
備註已經寫在 `larch/RELEASES.md` 的 release 4。**市集目前仍是 release 3 的凍結快照。**

## 二、調查篇

`project-d2fea918-c0eb-4ab6-aefb-2fe9a75dc7c4`。**還沒上架。**

- 開場選模式已經在正式專案上：`inv-724`「開場：這一輪要怎麼玩」，`mode` 預設 `free`
- 劇情模式：調查板與選單只開攻略那一格、劇情裡的選擇卡只有該選的按得下去、
  第一頁六格自動填（`board.html` 的 `autoPage1()`）
- 兩種模式各跑過一輪自動玩家：劇情模式名單 6/6、第一頁六格全填、選單 47 張全部一格；
  自由探索名單 6/6、第一頁全空、選單格數 1 到 9 都有
- `railcheck.py` 拿實跑逐字稿逐步比軌道，劇情模式 47/48（差的一步是擲骰不是 bug）

## 三、這一批背景（28 張）

**問題**：十個地點的白天與晚上原本是兩個不同的建築（各自獨立生成），
而且畫風是日式的，但設計稿 2026-09-07 就拍板寫實南港（`design/調查篇-場景.md` 86 行）。

**白天定案十張**（`art/bg-day-final/`）：
一樓、頂樓收音機店、手辦店用**舊圖**；街、站牌、捷運出口、便利商店、材料行、
洗衣店、斑比工作室用**新生的**。這是 yazelin 一張一張挑的。

**晚上與深夜十七張**：**拿對應的白天那張當唯一 `refs`**，prompt 不重寫場景，
只寫「Repaint this scene at EARLY EVENING / THREE IN THE MORNING，
Keep the layout of the reference image EXACTLY，ONLY the lighting changes」。
manifest 在 `art/bg-evening-night.json`。

一樓、頂樓、斑比工作室的**深夜不生**，那三個刻意共用正篇素材
（`bg-apartment-hall`、`bg-noah-shop`、`bg-bambi-studio`）。

**為什麼要這樣生**：三個時段各生各的就是當初壞掉的原因。我一開始又犯了一次
（三個地點各生三張），是 yazelin 指出來才改成「白天定稿→帶白天生晚上深夜」。

## 四、還沒 commit 的

`art/bg-day-final/`、`art/out/` 的新圖、`art/bg-*.json` 那幾份 manifest、
`交接-2026-09-12.md`（這一份）。`art/out/` 已經 199MB，**新圖進 repo 之前要先縮**
（見 `feedback_shrink_images_before_commit`）。

## 五、今天定下來的規矩

- **負控制要兩個方向。** 檢查器只在「該叫的時候叫」不夠，還要證明「不該叫的時候不叫」。
  `score.py` 的通關判準就是這樣抓到的（改了收工條件之後永遠印 ×）。
- **改完要重跑實玩才發。** 靜態檢查過不代表玩家走得完。正篇那趟 1118 張卡就是為這個跑的。
- **代號要對得到檔案。** 圖生了但版子上沒用到會靜默發生：`bg-studio-day` 跟正篇
  `assets.json` 撞名，`push.py` 的查找順序第二步就中，本機那張永遠上不去，
  玩家看到的是正篇那張直播間。推之前要逐一對代號。
- **`PUT /projects` 會清空版子**，流程是抓版子→PUT→推回去→回讀比對。
- **Larch 的 ETag 是專案層的版本號**，所有版子共用一個；還不存在的版子拿別塊的 ETag 來建。
- **重建版子會把線上的 `data.localizations` 整批刪掉而且不報錯。**
- **不要 `git add -A`**，會掃進別的 session 未提交的東西（踩過兩次：agent worktree、`larch/stickers/`）。

## 六、工作目錄現在的狀態（**做到一半，先讀這一段**）

背景換圖正在進行，推第二次中。

**刪除狀態的 24 個檔**：`art/bg-investigation/` 底下的舊 `.jpg`。
新圖是 `.png`，同名不同副檔名，所以裝新圖的時候把舊的 unlink 掉了。
**那是預期中的**，不是意外，不要還原。

**未追蹤的**：art/bg-booth-evening.json, art/bg-day-final/, art/bg-day-round1.json, art/bg-evening-night.json, art/bg-investigation/bg-bambi-studio-day.png, art/bg-investigation/bg-booth-evening.png 等

**還沒 commit**：上面兩批、`art/bg-day-final/`（十張定案的白天）、
`art/bg-*.json` 那幾份 manifest、這份交接。
`art/out/` 已經 199MB，**新圖進 repo 之前要先縮**。

## 七、scratchpad 裡還沒進 repo 的東西

路徑是 `/tmp/claude-1000/-home-ct/7955990f-7a10-435a-ab8b-923901aff325/scratchpad/`，
**session 綁定，壓縮之後不會記得去看，所以列在這裡**：

- `state-backup.json` — `larch/inv/state.json` 動手前的備份。
  我把 27 個代號從它的 `assets` 拿掉才能讓新圖重新上傳（見下一段）。
- `main-project-backup.json` — 正篇整包快照（動 CG 之前），1.4 MB
- `score.py` / `pool.py` / `relayout.py` 等驗收腳本
- `main-run2/`、`m-story/`、`m-free/` 等自動玩家逐字稿

## 八、今天踩到但還沒寫進別處的一課

**驗上傳要看線上網址的時間戳，不是看本機有沒有檔案。**

第一次推背景的時候我做了前置檢查「每個代號都對得到本機檔案」，36 個全過，
`missing_bg` 空、回讀比對「一致」，看起來完全成功——**實際上新圖一張都沒上傳**。

原因是 `push.py` 的查找順序：

```
1. 這個專案上傳過的（state.json 的 assets）  ← 第一步就中
2. 正篇的 R2 網址
3. 本機 art/ 上傳
```

代號早就在 `state.json` 裡有網址，它根本不去看本機檔案換了沒。
**換圖之後要先把那些代號從 `state.json` 的 `assets` 拿掉**，下一次推才會重傳。

順帶也是靠這個檢查才發現 `bg-booth-evening`（錄音間門口的晚上）
從來沒生過——板子指著一個不存在的代號，白天與深夜都用 `bg-booth-hall`。
已經補生（拿 `bg-booth-hall` 當 refs），所以這一批實際是 28 張不是 27 張。

## 九、這一輪的基準數字（有人懷疑被動過就對這組）

第二次推背景完成後回讀（2026-09-12 04:26）：

| 專案 | 卡片 | 邊 | 帶條件的邊 |
|---|---|---|---|
| 調查篇 `board-main` | 770 / 770 | 866 / 866 | 249 / 249 |
| 調查篇 謝幕版子 | 6 / 6 | 5 / 5 | — |

回讀比對結果「一致」。正篇是八塊板子，數字在 `larch/RELEASES.md` release 4。

**這組數字的用途**：`PUT /projects` 會清空版子、編輯器分頁會把版子打回舊版，
兩種情況都不報錯。任何人動過之後對不上這組，就是被蓋掉了。

## 十、還沒做的（接手從這裡開始）

背景這一輪的四項驗收還沒跑：

1. 八塊板子的卡片／邊數跟上表一致
2. **28 個代號逐一查線上網址的時間戳**（不是查本機檔案，理由見第八節）
3. `larch/verify.py` 全綠
4. 抽三個地點，把白天／晚上／深夜三張並排看是不是同一個空間

更遠的：配音 1446 句、BGM、音效、表情差分、調查篇上架、CG 跨周目解鎖。
