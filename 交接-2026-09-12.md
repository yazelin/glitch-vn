# 接手筆記（2026-09-12）

這一份是給壓縮之後的自己、或別的 session 接手用的。**只寫還沒落進其他文件的東西**，
機制與規格看 `README.md`、`design/調查篇.md`、`larch/RELEASES.md`。

## 一、正篇《格莉奇與黑洞先生》

`project-bec1644c-0dfe-4447-86c0-0c592e2f939f`。**這一輪的東西都做完了，等 yazelin 發市集。**

- CG 收藏十四張改成走到那一場才解開（原本開場就全看得見，等於劇透）
- 「聯動・猜歌」收的圖換成第三章真正用的 `bg-collab-studio`
- 白板七章重排成蛇行網格（**只動 position**，卡片 id 沒變，讀者存檔不受影響）
- 第四章一處立繪：黑洞先生從 `farRight` 移到 `left`，原本跟格莉奇疊在同一格
- 拿掉專案上殘留的日語設定（只有第一章被翻過 81 張，其餘六章沒有）

驗過：`larch/verify.py` 全綠、Playwright 從標題玩到謝幕 1118 張卡沒卡住、
十四張 CG 各在該解的地方解開、走到一半只解七張時另外七格仍是鎖的。

**已經發了。** yazelin 在 2026-09-11 18:33:47Z（台北 09-12 02:33）發佈，
市集上那筆 `updateLog` 標的是 **version 3.1**，內容就是這一輪的五項。
`releaseNumber` 是 4（累計第幾次發佈），所以我的 `RELEASES.md` 叫 release 4、
市集叫 3.1，**對外溝通要用 3.1**。

`POST /publish` 填不進更新說明，要在網頁後台第 ⑤ 區貼，所以發佈這一步是人做的。

### 市集掛的是哪一版：不要猜，打這個端點

`https://larch.ink/api/marketplace`，**不帶認證就能打**（公開）。回傳的 `games`
陣列裡找 `title`，每一筆有 `releaseNumber`、`updateLog`（每次發佈的版號與備註）、
`publishedAt`、`updatedAt`、`nodeCount`、`publishedBoardIds`、`views`／`plays`。

2026-09-12 對過的一組：

| 欄位 | 值 |
|---|---|
| id | `a2a10427-7326-4a86-b806-c2476fc1c22a` |
| publishedAt | 2026-08-23T02:48:01Z |
| updatedAt | 2026-09-11T18:33:47Z（台北 09-12 02:33） |
| releaseNumber | 4（`updateLog` 最後一筆是 version 3.1） |
| nodeCount | 638 |
| publishedBoardIds | ch01…ch08 全部八塊 |
| views／plays | 902／272 |

**`nodeCount` 638 就是八塊板子的合計**（84＋69＋88＋81＋77＋90＋144＋5），
跟編輯專案一字不差——市集與編輯專案是同一份，所以在編輯專案上做的實玩驗證
對玩家是有效的。**正篇是 638 卡，不是 725；725 是調查篇 `build.py` 的輸出**
（2026-09-12 我把這兩個數字搞混過一次，報成「正篇 725 卡」）。

## 二、調查篇

`project-d2fea918-c0eb-4ab6-aefb-2fe9a75dc7c4`。**還沒上架。**

- 開場選模式已經在正式專案上：`inv-724`「開場：這一輪要怎麼玩」，`mode` 預設 `free`
- 劇情模式：調查板與選單只開攻略那一格、劇情裡的選擇卡只有該選的按得下去、
  第一頁六格自動填（`board.html` 的 `autoPage1()`）
- 兩種模式各跑過一輪自動玩家：劇情模式名單 6/6、第一頁六格全填、選單 47 張全部一格；
  自由探索名單 6/6、第一頁全空、選單格數 1 到 9 都有
- `railcheck.py` 拿實跑逐字稿逐步比軌道，劇情模式 47/48（差的一步是擲骰不是 bug）

### 已知缺口：手機第 9 到 14 天沒有新內容

四個分頁（訊息／格莉奇／直播／電話）裡：

- **貼文只寫到第八天**。七則，落在第 1、1、2、3、4、6、8 天
  （`design/調查篇-手機.md` 的貼文表，`push.py` 的 `phone_feed()` 讀它）。
  貼文是累積顯示（`x.day <= day`），所以第九天起那一頁一則都不會再增加。
- **直播只有第 2、5、8 天**（`phone.html` 的 `LIVE_DAYS=[2,5,8]`，
  跟板上那三張插播一致）。第九天起永遠是「上次開台：第 8 天。／
  聊天室最後一則：這集有我。」
- **電話那一頁是故意永遠空的**（`design/調查篇-手機.md` 七十二行）。
  「通話紀錄是空的。沒有人會打來。」是這件道具的用途，不是缺漏。
- 訊息那一頁還會動：斑比那則掛 `open_studio`、公關窗口那則掛 `met_櫃檯`，
  是旗標驅動不是天數驅動，所以可能落在第九天之後。

所以**第九天到第十四天，六天，手機大致是凍住的**，而那是全篇最後 43%，
玩家最常翻手機找線索的一段。成因跟市集簡介寫「十一天」是同一批漏網：
手機的內容是照十二天（或更早）那版寫的，2026-09-09 拉到十四天時沒跟上。

**補這個要寫東西不是改程式**，貼文是她的語氣、留言是那六個 ID 的謎題的一部分
（`調查篇-手機.md` 四），所以等 yazelin 決定要不要寫以及誰來寫。
表格格式是 `| 天 | 貼文 | 指向 | 底下的留言 |`，`push.py` 會自己讀。

## 三、這一批背景（28 張）

**問題**：十個地點的白天與晚上原本是兩個不同的建築（各自獨立生成），
而且畫風是日式的，但設計稿 2026-09-07 就拍板寫實南港（`design/調查篇-場景.md` 86 行）。

**白天定案十張**（`art/bg-day-final/`）：
一樓、頂樓收音機店、手辦店用**舊圖**；街、站牌、捷運出口、便利商店、材料行、
洗衣店、斑比工作室用**新生的**。這是 yazelin 一張一張挑的。

**晚上與深夜十七張**：**拿對應的白天那張當唯一 `refs`**，prompt 不重寫場景，
只寫「Repaint this scene at EARLY EVENING / THREE IN THE MORNING，
Keep the layout of the reference image EXACTLY，ONLY the lighting changes」。
manifest 在 `art/bg-evening-night.json`。

一樓、頂樓、斑比工作室的**深夜不生**，那三個刻意共用正篇素材
（`bg-apartment-hall`、`bg-noah-shop`、`bg-bambi-studio`）。

### ★ 要 yazelin 決定：那三個共用的深夜，跟白天晚上不是同一個空間

2026-09-12 用眼睛逐一比對過（十二個地點各三張並排），**八個完全正確**
（捷運出口、車站前那條街、便利商店、自助洗衣店、材料行、車站前站牌、
手辦店、十四樓大廳）：同一個空間、同一個機位、只有光線與窗外變。

**共用正篇素材的那三個不是。** 不是時段不對，是**看起來就是別的地方**：

| 地點 | 白天／晚上是什麼 | 深夜換成什麼 | 違和在哪 |
|---|---|---|---|
| 一樓 | 信箱牆、上樓的階梯、牆邊一張折疊椅，米黃與橘的舊公寓大廳 | `bg-apartment-hall.jpg`（正篇）：紫光的走廊，正對一扇公寓門，地上一個紙箱 | **機位與空間都換了。** 白天晚上是站在大廳看信箱牆，深夜變成站在某層樓的走廊看別人家的門。信箱牆、階梯、折疊椅全都不在畫面裡 |
| 頂樓收音機店 | 從外面看那間鐵皮加蓋，門開著，看得到裡面的工作檯與外面的水塔、曬衣繩 | `bg-noah-shop.jpg`（正篇）：紫光，從**屋內**看，滿牆收音機與一張工作檯 | **裡外顛倒。** 白天晚上人在門外、深夜人在屋內。嚴格說還是同一間店，但玩家看到的是另一個機位，接不起來 |
| 斑比工作室 | 鐵捲門的車庫工作室：白色背景布、長桌、一排螢幕、木箱、高腳椅，門外是巷子 | `bg-bambi-studio.jpg`（正篇）：紫光的房間，有窗簾與窗外夜景、公仔架、辦公椅 | **完全是另一個房間。** 三者之中最明顯。鐵捲門、背景布、木箱、巷子一樣都沒有 |

**為什麼會這樣**：那三個「深夜共用正篇」是更早的決定，而這一批圖的存在理由是
「同一個地點的三個時段必須是同一個物理空間」。兩件事衝突，
我當時照著做沒有回頭檢查。

**補的話**：跟其他七個同一個做法，拿對應的白天當唯一 `refs`，
prompt 寫「Repaint this scene at THREE IN THE MORNING」。一張約三分鐘，
三張加上推一次與重驗大約半小時。**沒動，等 yazelin 決定**，
因為那是改一個已經拍板過的設計。

### 錄音間門口的深夜是白天那張（但玩家看不到）

`bg-booth-hall.jpg` 同時掛在白天與深夜，窗外是大白天的藍天——
深夜三點的走廊會有日光。但那一格 `sealed: true`
（`larch/cards/board.html` 第 203 行），**永遠是灰的、進不去**，
是刻意的挫折設計。兩趟實玩的逐字稿裡「錄音間門口」出現 0 次。
所以這張錯的圖玩家看不到。哪天 booth 開放要記得補。
（2026-09-12 補生的 `bg-booth-evening.png` 也是給這個進不去的地方用的。）

**為什麼要這樣生**：三個時段各生各的就是當初壞掉的原因。我一開始又犯了一次
（三個地點各生三張），是 yazelin 指出來才改成「白天定稿→帶白天生晚上深夜」。

## 四、版控狀態（2026-09-12 收乾淨了）

**工作目錄是空的，全部在 origin 上。** 這一輪的 commit：

```
822cd18 state.json：記下這一批 25 張背景的線上網址
d28634a 缺背景的閘門修對範圍，並讓「用別的時段代替」不再靜默
c886435 手機卡：回應列的送出鈕畫出來，並修它在每一頁都露出來
1601844 推送不再把副檔名寫死，缺背景就擋下來，並加成常設檢查
0941e45 調查篇背景換成同一場景的白天／晚上／深夜（28 張）
996d3dd 表情包素材進版控：格莉奇・偵探小說家（已上線 Larch 市集）
3878bba 天數對帳：補上十二天拉到十四天時沒跟上的數字，並加成常設檢查
```

- `larch/stickers/`（已上線市集的表情包，一整天只在硬碟上）已進 git，5.9 MB
- `art/bg-day-final/`（十個檔 21 MB）**捨棄了**：跟 `art/bg-investigation/`
  同名檔逐位元相同（`cmp -s` 十個全過，刪之前驗了兩次）。
  定案的圖在 `art/bg-investigation/`，挑了哪十張看 `art/bg-day-manifest.json`
- 24 個 `.jpg` 的刪除是預期的，每一個都有同名 `.png` 接上（逐一比對 24/24）
- `.git` 現在 724 MB，圖佔大部分。要瘦身是另一件事，沒動

## 五、今天定下來的規矩

- **負控制要兩個方向。** 檢查器只在「該叫的時候叫」不夠，還要證明「不該叫的時候不叫」。
  `score.py` 的通關判準就是這樣抓到的（改了收工條件之後永遠印 ×）。
- **改完要重跑實玩才發。** 靜態檢查過不代表玩家走得完。正篇那趟 1118 張卡就是為這個跑的。
- **代號要對得到檔案。** 圖生了但版子上沒用到會靜默發生：`bg-studio-day` 跟正篇
  `assets.json` 撞名，`push.py` 的查找順序第二步就中，本機那張永遠上不去，
  玩家看到的是正篇那張直播間。推之前要逐一對代號。
- **`PUT /projects` 會清空版子**，流程是抓版子→PUT→推回去→回讀比對。
- **Larch 的 ETag 是專案層的版本號**，所有版子共用一個；還不存在的版子拿別塊的 ETag 來建。
- **重建版子會把線上的 `data.localizations` 整批刪掉而且不報錯。**
- **市集／線上掛哪一版，打端點對，不要推論。** 我寫過「市集仍是 release 3 的
  凍結快照」，那是沒驗證的推測，實際上 09-12 02:33 就發了 3.1。
  查法在第一節。標明「這是推測」是對的，但能查的就該去查。
- **看 log 不要只看尾巴。** `missing_bg` 有列出 20 筆，就在推送 log 的第一行，
  而最後幾行是「回讀一致」。我跑 `tail -5` 就回報成功。
  現在那道閘門擋在 `PUT` 之前，不再只印在讀不到的地方。
- **兩個數字長得像的時候先確認是哪個專案的。** 正篇 638 卡、調查篇 725 卡，
  我把調查篇 `build.py` 的輸出當成正篇報過一次。
- **驗收之前先問：這一輪有沒有把「真正要決定的事」放進被比較的東西裡。**
  2026-09-12 兩個人用兩種方式漏掉同一件：五個角色的配音試音，題目裡只有
  Larch 的 A 與 B，跑代聽的人只驗了題目問的，出題的人（我）沒把設計稿
  指定的本機聲線放進去——而「要不要換掉設計稿配好的那個」才是真正要決定的事。
  票數再漂亮，比錯對象就是零。
- **通過率不等於覆蓋率。** 同一天差點又中一次：配音第二輪「403 句沒通過驗收」
  看起來像大回歸，實際上那是重試已知難切的句子，失敗就保留原檔；
  真正的數字是「已生」從 865 變 951。**要看的是有幾句有檔，不是有幾次通過。**
- **一個看起來完整的解釋，會把不屬於它的東西一起吸收掉。**
  回報缺口的時候，數字要能**逐項對到原因**，對不上的那幾個單獨列出來，
  不要併進最近的那個解釋裡。2026-09-12 中過：配音「還缺 64 句」，
  其中 60 句是沒有參考音的路人——那個解釋有名字、有原因、看起來完整，
  剩下 4 句就順勢被算進去了。逐句查才發現是純刪節號（「……」），
  代表的是一個沒人發現的規則缺口，不只是四個檔案。
  如果只回報「還缺 64 句，都是路人」，那四句就永遠消失了。
- **基準的產生指令必須完整記錄，一個輸入都不能漏。**
  漏掉的那個一定是最關鍵的——不關鍵的漏了也看不出來。重產通關路線時
  連漏兩個（PREFER、FILLPAGE1），兩次都是靠逐字稿的痕跡反推，
  而反推會失敗：下一個變數可能不留痕跡，那時看起來只會像「路線又變了」。
  所以清單寫進產物本身（`design/調查篇-通關路線.txt` 的檔頭列十個變數，沒設的也寫）。
### 手上那個數字，量的是我們以為的那件事嗎

**2026-09-12 一天之內五次追錯方向，錯法同一個。** 五行並排的價值在於它們長得不一樣、
追查方向也不一樣——所以看單一一次不會發現這是同一個結構。

| 當時以為 | 手上那個數字實際量到的 |
|---|---|
| BGM 尾巴壞掉（平坦度 0.06–0.90） | **靜音**。每支曲子都淡出收尾，靜音的頻譜平坦度必然接近 1，底噪就是平的 |
| 代聽說 B 比較老，所以 B 比較接近四十幾歲 | **相對量**。知道 B 比 A 老，不知道兩個各自落在哪 |
| urls.json 有 57 條死網址，推上去會 404 | **舊表**。台詞改過檔名雜湊就變，那 57 條板上沒有卡片會引用 |
| 設計稿記保全 59.9 Hz，比誰都低 | **雜訊**。參考音 4.6 秒、−35 dBFS，pyin 鎖底；16 秒長檔量出來是 220–240 的女聲 |
| 生成十五次有一次沒拿到 voiceUrl | **量不到被讀成東西不在**。檔案一直都在，是一句 1.10 秒的短台詞 pyin 一幀都抓不到 |

**可操作的反射動作**：拿到一個數字要下結論之前，先問「這個量在這個位置上還有沒有意義」。
靜音處的頻譜、樣本外的推論、舊表、偵測範圍邊緣、量不到與不存在——這五種都會給你
一個看起來正常的數字。

**而且工具不可以讓「量不到」跟「東西不在」共用同一個回傳值**
（`tools/check_cast_f0.py` 的 `f0()` 現在回 `(中位數, 幀數)`，量不到回 `(None, 幀數)`）。
修完之後才看見：每個角色都有一批句子量不到（管理員抽 25 句有 8 句），
先前被默默丟掉，所以報出來的「25 句的中位數」其實是 17 句的。

**這個形狀不是今天才有的。** `tools/publish_voice.py` 檔頭早就寫著
「要比內容，不可以只比大小——統一響度之後有些檔大小剛好一樣，用大小判斷就整批跳過，
線上還是舊的聲音（實際發生過）」。那是別人踩過同一個形狀之後寫下來的。

### 一個走完整圈的案例：`bgm-laundry` 這一槽

**六次判斷，前五次都有一個看起來充分的解釋，真因在第六次才拿到。**
留這一段是因為每一步的錯長得都不一樣——看單獨一次都像是合理的推進。

| 第幾次 | 當時的動作 | 當時的結論 | 實際上 |
|---|---|---|---|
| 一 | 負控制「答錯」 | 有人把 A B 寫反了 | 檔案沒反（程式、日誌、雜湊鏈三層驗過） |
| 二 | 量頻譜平坦度 | 檔案跟描述對不上 | 量的是全長、聽的是節錄，兩邊不是同一份素材 |
| 三 | 查到尾段 0.8975 | B 的尾巴壞掉，要重產 | （下一行推翻） |
| 四 | 把音量一起量 | — | 每支曲子都淡出收尾，**靜音的平坦度必然接近 1** |
| 五 | 查 prompt 對產出 | 產製服務沒照 prompt 做 | 對的，但這不是代聽答錯的原因 |
| 六 | 重下委託之前先量 | — | **三個量的差距只有 1.5–1.7 倍，素材本身太接近** |

**真因**：代聽在 1.5 倍的差距上沒有著力點。`store` 那種分得開的對子是起音密度
差 6.67 倍（80 對 12）；`laundry` 只有 2.29 倍（96 對 220）。
兩個方向相反的負控制（亮度答案 A、雜訊感答案 B）都沒答出來——**儀器沒壞，素材太像。**

**可帶走的**：
- 送代聽之前先量。差距沒有拉開到已知分得開的那種量級，就不要送，那只會燒一輪。
- 「負控制答錯」有三種可能而且長得一樣：儀器壞了、標準答案錯了、素材太接近。
  一次只能排除一種，而前五次每一次都以為自己排除的是最後一種。

- **不要 `git add -A`**，會掃進別的 session 未提交的東西（踩過兩次：agent worktree、`larch/stickers/`）。

## 六、背景這一批推完了，驗過了

2026-09-12 08:30–08:43 推上調查篇，四項驗收：

| 驗什麼 | 結果 |
|---|---|
| 線上網址的**時間戳** | 38 種背景，其中 **25 種是這一批**（09-12 08:30–08:43） |
| 背景是空字串的場景卡 | **0 張**（推之前是 22 張） |
| 卡片／邊／帶條件的邊 | 770／866／249，跟第九節的基準一字不差 |
| `verify.py`（正篇） | 全部通過 |
| `tools/pathlint.py` | 0 件，負控制 11 項 11/11 |

標成舊的 13 種是該沿用的：`bg-apartment-hall`／`bg-bambi-studio`／
`bg-noah-shop` 共用正篇；`bg-lobby-day`／`bg-roof-day`／`bg-figure-day`
是 yazelin 選的舊圖；`bg-tower14` 三張與 `bg-desk-night`、`bg-catgrass-*`、
`bg-booth-hall` 不在這一批。

## 七、scratchpad 裡的東西（session 綁定，壓縮後找不到）

路徑 `/tmp/claude-1000/-home-ct/7955990f-7a10-435a-ab8b-923901aff325/scratchpad/`。
**該進 repo 的都進去了**，這裡剩的是備份與逐字稿，丟掉不影響：

- `inv-before-bgpush.json` — 調查篇動背景之前的整包快照（2.3 MB）
- `main-project-backup.json` — 正篇整包快照（動 CG 之前，1.4 MB）
- `cbc-backup.json` — 測試專案動略過探針之前的快照（已還原回去）
- `probe.py` / `larchapi.py` — 略過探針，要重驗的話還在
- `push-bg*.log`、自動玩家逐字稿

## 八、今天踩到但還沒寫進別處的一課

**驗上傳要看線上網址的時間戳，不是看本機有沒有檔案。**

第一次推背景的時候我做了前置檢查「每個代號都對得到本機檔案」，36 個全過，
`missing_bg` 空、回讀比對「一致」，看起來完全成功——**實際上新圖一張都沒上傳**。

原因是 `push.py` 的查找順序：

```
1. 這個專案上傳過的（state.json 的 assets）  ← 第一步就中
2. 正篇的 R2 網址
3. 本機 art/ 上傳
```

代號早就在 `state.json` 裡有網址，它根本不去看本機檔案換了沒。
**換圖之後要先把那些代號從 `state.json` 的 `assets` 拿掉**，下一次推才會重傳。

順帶也是靠這個檢查才發現 `bg-booth-evening`（錄音間門口的晚上）
從來沒生過——板子指著一個不存在的代號，白天與深夜都用 `bg-booth-hall`。
已經補生（拿 `bg-booth-hall` 當 refs），所以這一批實際是 28 張不是 27 張。

## 九、這一輪的基準數字（有人懷疑被動過就對這組）

第二次推背景完成後回讀（2026-09-12 04:26）：

| 專案 | 卡片 | 邊 | 帶條件的邊 |
|---|---|---|---|
| 調查篇 `board-main` | 770 / 770 | 866 / 866 | 249 / 249 |
| 調查篇 謝幕版子 | 6 / 6 | 5 / 5 | — |

回讀比對結果「一致」。正篇是八塊板子，數字在 `larch/RELEASES.md` release 4。

**這組數字的用途**：`PUT /projects` 會清空版子、編輯器分頁會把版子打回舊版，
兩種情況都不報錯。任何人動過之後對不上這組，就是被蓋掉了。

## 十、還沒做的（接手從這裡開始）

1. **手機第 9 到 14 天的內容**（第二節那個缺口）。等 yazelin 決定要不要寫、誰寫。
2. **調查篇上架。** 板子與背景都齊了，`pathlint` 綠。上架只能在網頁按。
3. **glitch-live 的深色手機 UI 整合。** 它自己的 `verify.mjs` 18 項全過，
   包含「`push.py` 要換的 5 個字串在 `card.html` 裡都找得到」。
   卡在三件要 yazelin 拍板的（第五天留言橋段怎麼接、循環影片放哪、
   公關窗口那通沒打來的電話要不要收成正典），見 `~/glitch-live/NEXT.md`。
   **整合是整份覆蓋**，所以 2026-09-12 修在 `phone.html` 的送出鈕箭頭
   會被蓋回去，那邊要一起補（`card.html` 也是空的 `<i>`）。
4. 更遠的：配音 1446 句、BGM、音效、表情差分、CG 跨周目解鎖。

## 十一、Larch 平台這一天學到的四件

1. **版子內容要走 `PUT /projects/{id}/boards/{bid}`。** 整包 `PUT /projects`
   只改得到 `boards[]`，而**播放器讀的是專案物件頂層的 `nodes`／`edges`**，
   所以只推專案的話「讀回來是對的、玩起來是舊的」。
2. **`miniGameSkippable: True` 單獨無效**，要配 `miniGameFrame.showButton: True`
   才會冒出「略過小遊戲」。按下去會繞過卡片自己的 `larch:set`，
   所以手機卡那種「關掉時要寫回變數」的設計，略過就不會寫。
   手機卡寫的是 `showButton: False`，玩家按不到，**誰打開這條就變成真的**。
   實測：略過後 `open_phone` 留在 `true`，下一次 `BAG_CLEAR` 清得掉。
3. **插播是邊緣觸發不是位準觸發。** `interruptOnce: False` 加上條件維持成立，
   它不會重複發動——所以「略過之後會被關在無限迴圈」是錯的
   （我推論過，實測否定）。
4. **市集掛哪一版打 `https://larch.ink/api/marketplace`**（公開，免認證），
   見第一節那張表。

## 十二、一個永遠不會紅的負控制

《規格-配音與BGM掛回板子.md》第四節第三項要求「建置時故意把 `_voice()` 的 key
少帶 emotion，M 應該暴增」。實跑的結果是 M 一句都沒變：進去 6 句，出來 6 句。

原因不在程式。調查篇這塊板上 emotion 只有三種值——`''` 1310 句、`None` 588 句、
`描述動作` 198 句。前兩種在 `voice.key()` 的雜湊裡都寫成空字串，是同一件事；
第三種是舞台指示，設計上就不配音，本來就被排除。**被弄壞的那個欄位，
在這份資料上沒有區別度。** 改成少帶講者才咬得住：1397 → 0。

**一個永遠不會紅的負控制，跟沒有負控制不一樣，它更糟——它會讓人以為驗過了。**
失效的原因不是誰寫錯程式，是設計負控制的時候沒有先確認那個欄位在這份資料上
有沒有變化。所以下負控制之前多一步：**先數一次那個欄位的相異值。**
只有一種值的欄位，弄壞它等於沒弄壞。

這一條跟第五節那句「手上那個數字，量的是我們以為的那件事嗎」是同一個形狀，
只是換到負控制這一層：負控制也是一個量測，它一樣會量到不是它以為的那件事。

規格已經照實改掉並標明是誰在什麼情況下發現的（`~/glitch-live` commit e344b8e）。

## 十三、錄音帶卡那 8 句從來沒被收走過

`tools/gen_voice.py` 的 `utterances()` 走的是建置出來的卡片，所以照理說板上有的
它都收得到。實際上錄音帶那幾張卡的 8 句不在它收到的清單裡：

    旁白「錄音機轉了一下。」×5（inv-tape-rec_{noah,bambi,clerk,parts,guard}-pre）
    諾亞、斑比、材料行老闆的錄音內容各 1

**這跟路人那 184 句不是同一件事，不要併在一起算。** 路人是選好音色了、音色在
Larch、還沒生；錄音帶卡這 8 句是**從來沒有被當成要配音的句子收走過**，所以
`urls.json` 裡連代號都沒有，`check_cast_f0` 那類工具也掃不到它們。

原因還沒查。要查的方向是 `utterances()` 讀的是哪一份建置結果——它可能讀的是
正篇那條線的板子，而錄音帶卡是調查篇在 `Board.add()` 時直接組出來的。

下一輪要做的事：確認原因、把這 8 句收進去、生、發佈、重掛。做完之後
storylint 的 S3 缺漏會從 431 掉到 423。
