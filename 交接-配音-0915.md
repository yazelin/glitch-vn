# 配音補完 2026-09-15 04:00–04:30（另一條線寫的）

接手時 `voice_batch.py` 已經在跑，這份記的是那之後發生的事。**沒有推任何東西上 Larch。**

## 做完的

- **旁白 603 句生成完成**：4.7 秒一句，總長約 47 分鐘，603 個檔全部產出，沒有小於 8 KB 的壞檔，中位 103 KB。
- **響度統一、頭尾靜音、發佈**：由另一個 session（scratchpad `7955990f`）的收尾管線跑完，印出 ALLDONE。
- **對帳通過**：`art/voice` 2029 個、`docs/voice` 2029 個，大小全部相同，抽驗 60 個內容雜湊也相同。

## 修掉的坑：整段無聲的檔會讓 level_voice 每次都失敗

`tools/level_voice.py` 對 0.5 秒以內的氣音、純標點那種檔，第一段量出來是 `input_i = -inf`，
餵進第二段就變成 `offset=inf`，ffmpeg 直接失敗、不產暫存檔，於是印「壓失敗，原檔留著」。
**因為沒進帳本，之後每一次重跑都會再失敗一次**，看起來像有 24 個檔壞掉。

改成量到 `-inf` 就記帳跳過。驗證方式：改之前每跑一次都印 24 行壓失敗，改之後第一次印
「整段幾乎無聲，跳過」並記帳，第二、三次跑完全沒有任何訊息（`grep -c` 為 0）。

## 兩條同時跑會互相殺掉暫存檔

我一開始不知道那條收尾管線在跑，重複執行了一次 `level_voice.py`。兩個行程的暫存檔名
**是同一個**（`<檔名>.tmp.mp3`），先改名的那個會讓另一個找不到自己的暫存檔。
當時 24 個「壓失敗」裡有 16 個其實已經壓好。

**備份沒有被污染**（這是真正危險的地方，因為它是從 `pre-level/` 讀檔去壓的）：
對照 `levelled.json` 掃過那段時間被重寫的 508 個備份，沒有一個內容等於壓過的版本。

## 調查篇的語音其實已經補完

板上還缺音檔的只剩 6 句，每一句都**不應該配音**：

| 講者 | 內容 |
|---|---|
| 玩家、斑比、貓草 | `……`（純標點，送進引擎會在 `torch.cat()` 爆掉） |
| 玩家 | `（答什麼都可以）`（舞台指示，09-14 那個 commit 已經決定不唸） |
| 旁白、玩家 | `{{page1_text}}`、`{{list_text}}`（模板變數） |

這六句都沒有 `urls.json` 的網址，所以掛載之後是安靜的，不是壞的。

## 還沒處理的地雷：87 筆網址指向不存在的檔

`urls.json` 有 2116 筆，其中 **87 筆的檔在 `art/voice` 與 `docs/voice` 都不存在**，
而且**這 87 個代號全部都還被現在的台詞用到**。掛載之後那 87 句會拿到 404 的 `voiceUrl`
——卡片看起來有聲音，播起來沒有，比沒有聲音更難發現。

分佈：全部在正篇 ch01 到 ch07，角色是黑洞先生 50、諾亞 26、鐵塔 17。
**這三個是外部長檔配音的角色，不可以用本機補**（補了就是另一個人的聲音）。
調查篇一筆都沒有。

要嘛把那 87 句的錄音找回來，要嘛把那 87 筆從 `urls.json` 拿掉讓它們安靜。**這要本人決定。**

## 掛載沒有做，而且不能照現在的建置直接推

`larch/inv/build.py` 這次跑出來是**節點 724、邊 840**，線上的板子是 770（加 credits 是 776）。
對不上就不能推，`PUT /projects` 會清空版子而且不報錯。快照在 `快照/調查篇-2026-09-12-掛載前.json.gz`。

另外建置沒有印規格要求的「配音：板上 N 句　掛上 N 句　對不到 N 句」那三個數字，
照 `交接-2026-09-12.md` 第六條，掛載的收口已經搬到 `Board.add()`，這支只產 JSON 不掛聲音。

storylint 現在仍然是 **S3 紅：1828 句台詞、244 句沒有 voiceUrl**，那是因為還沒掛載。
