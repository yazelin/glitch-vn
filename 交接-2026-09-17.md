# 交接 2026-09-17 凌晨（他在 Meet 上對八個人直播試玩，邊玩邊修）

## 線上現在是什麼

- 調查篇專案 `project-d2fea918…`，主版子 809 卡／936 邊，樂園 26 張、CG 解鎖 13 張都在。**沒有整包重建過。**
- 講者名：鐵塔→經紀人、貓草→客人、諾亞→修收音機的（`push.py` 的 `DISPLAY`，全程固定）。
  貓草是 `@CatGrass_80`、諾亞是 `@Radio_Noah`，六個 ID 之二，卡片印本名等於印答案。
  **旁白正文還有 13 張寫「諾亞」、2 張寫「貓草」（手辦店那場），他還沒拍板要不要改。**
- 配音網址全部改走 jsDelivr（`novelkit.cdn()`）。urls.json 仍記 Pages 網址，掛卡時才換。
  改音檔後 `tools/publish_voice.py --push`，或手動 push 後 `--purge`，不然 @main 快取十二小時。
- 劇情模式軌道：目的地還沒解鎖時由軌道自己解鎖（board.html 的 railTo 那段），不再退成全開。
- 通關路線換回 9/12 的版本（`471435b`），`walk` 變數單獨 PUT 上線。
  Codex 改的那版留在 `design/調查篇-通關路線-codex改版-未採用.txt`。
- 筆記卡 `~~` 改組合字元刪除線；謝幕字卷副標改「十四天」。

## 全開的真因（已修，別再查一次）

Codex 改路線把第 2 天下午、第 3 天上午的便利商店換成一樓。店員「問店員一件事」要第三次以後才開
（`met_店員 >= 3`，第一天定稿 1358 行），第 3 天晚上那格變空，整條軌道往後滑：第 4 天晚上問不到
「這條街深夜還有什麼開著」→ `open_laundry` 沒設 → 第 5～8 天每晚軌道指洗衣店 → board.html railOff 全開
→ menu.html 對不到也放行 → 第 9 天工作室同理。修之前的版子用 `MODE=story` 自動玩家跑出來一模一樣。

## 沒做完的

1. **CG 排進劇情軌道**（他說「未來再弄更清楚、重新排整齊」）。靜態對照：9/12 路線上有 7 張劇情 CG
   （金色的帽子、斑比的牆、深夜的便利商店、兩分鐘＋第六次、我答應過他、她記得零件）；
   失物箱（幫他拿袋子）、保全的手機（保全四段：站在大廳等→再站一次→問他值幾點→聽他講）、
   兩顆蘿蔔（問店員每次都買一樣的那個客人）只有自由探索收得到。要排進去共需 6 格。
   Codex 那版是拿阿姨、南港站、手辦店、第 11～13 天晚上三次信箱去換前兩張——三次信箱疑似是目擊次數，
   動它會傷「四次」「第七行」。**排之前先弄清楚哪些格子是完美結局必需的。**
2. **自動玩家在修過的版子上一到 CG 解鎖卡就停住**（三輪都在第 6 天，沒印錯誤、沒進卡住偵測）。
   修之前的版子跑得完。沒查出來。人玩過得去。
3. 兩個檔案裡有這一輪的修正混著上一條線沒 commit 的改動，沒一起收：
   `larch/inv/build.py`（`~~`→U+0336）、`larch/inv/push.py`（DISPLAY＋推前讀線上對照，有外來卡就停；另一條線的手機通知順序 move_ops）。
   `larch/cards/board.html` 已收（2a2b101）。**push.py 沒 `--wipe-addons` 不會推**；真要整包推，推完照順序重跑
   `add_story_cgs.py → fix_park_cg_crossrun.py → add_relationship_cgs.py → replace_relationship_cg_art.py`。
4. 讀音：立著→豎著、單獨的錶→表、貓草「一顆不夠，三顆太多」改「煩但平」都已重配上線。
   「一顆不夠」那一版他還沒聽過。

## 04:00 之後補的（他回去睡了，交代「品質拉到最高、明天報告」）

- **正篇 87 個缺檔救回來了**：它們 8/22 就在 docs/voice，是上一條線清孤兒檔時在工作目錄刪掉、01:02 那次整批 commit 收進去的。
  從前一版 `git checkout` 還原（`f5ee…` 那批），不是重生。那四句短答也一起回來。
- **調查篇的諾亞、鐵塔、黑洞先生改用 Larch 的 MiniMax 音色逐句重生**（107 句；詼諧長者／廣播主持／紳士，跟正篇同一支）。
  `LARCH_VOICE` 登記了這三個人（gen_voice 從此不會在本機生他們）。`gen_larch_voice.py` 兩處修：線上講者是顯示名要換回本名
  才算得到代號；body 帶 `boardId`。**平台把最後一次 PUT 的版子當主線（activeBoardId），配音生成只在主線找卡**——
  patch_live 先推主版再推謝幕就把主線搶走了，全部 404；已把 activeBoardId 設回 board-main，patch_live 改成主版最後推。
  有 3 句含替身詞平台會照字唸（諾亞「。信箱」、鐵塔「唸」「零失誤」），要聽。
  **結果（05:00）**：107/107 生成、統一響度、發佈、推上去、快取清了（`49503e7`）。量過：諾亞中位 −19.0、鐵塔 −19.3、黑洞先生 −20.9 dB，
  只有鐵塔一句 −23.9 偏輕。第二輪重試時諾亞「……那是什麼。」平台逾時，留的是第一輪成功的那版。
- **遊樂園吃時段維持原設計**（05:30 作者確認；我一度改成不吃，已改回、線上也換回）。自動玩家不去遊樂園就好。
- **失物箱那張 CG 進了劇情軌道（05:40）**：第 2 天下午（原本只是去便利商店湊次數）→「信箱第三排那兩格」（trust_管理員 set 2）、
  第 9 天上午（站牌那場只設 `seen_aunt`，沒人再讀）→「電梯保養」（+1 → 3；**必須排在第 8 天「問管理員那台螢幕」之後，那格要 trust ≤ 2**）、
  第 10 天下午（南港站空走）→「幫他拿袋子」。sim_rail 對過：48 步全開、其他 CG 不掉、店員第三次仍在第 3 天晚上、櫃檯第六次仍在第 6 天下午。
  `walk` 已上線。commit `c838233` 的訊息說改了路線其實沒改（第 9 天那格被選單那行擋掉），真正的改在 `d7c26ed`。
- **建議先聽 6 句**已排進試聽頁（`試聽對照.html`）：一顆不夠、你來過六次、不是每天、諾亞「。信箱」、鐵塔「唸」「零失誤」。
  `listen.json` 是以代號為鍵的字典；`larch-labels.json` 要有 LARCH_VOICE 裡每個角色，不然 make_audition KeyError。
- **`tools/sim_rail.py`**：照劇情軌道走的變數模擬器（`--live <快照>` 把線上的 variableOps 蓋上來，管理員 trust 只在線上有）。
  現況：48 步全開得了（機率訪客在場時）、劇情 CG 來源卡走到 7/13；第七行／四次／這集有我／謝幕是插播與結局帶出來的，模擬不走那裡。
  機率訪客（5|3 店裡的鐵塔、10|2 一樓的黑洞先生）靠 board.html 的 pity 保底，模擬的 `--no-luck` 是最壞情況。
- **自動玩家在修過的版子上還是走不到底**：第七輪加了 60 秒沒動就截圖的看門狗也沒抓到——它是 6000 步跑滿正常結束，
  結束時畫面停在店裡「錄音機在包包裡。01 開錄音機／02 不開」那張選項卡。沒查出來。人玩過得去（他自己到第九天）。
- 響度：1440 句全量掃過，33 句短句另外用增益＋限幅拉齊（level_voice 的真峰值限制壓不到）；BGM 11 首差 2.7 dB、卡上一律 0.28。
- 線上驗證：1576 句 voiceUrl 全是 jsDelivr；線上代號對本機重建 0 句不同；抽 40 句下載比位元組 0 不同。
- 更新說明寫在 `larch/RELEASES.md`「調查篇 下一版」。**沒有按發佈**：remix 允許、活動標籤只有網頁看得到，agent API 發會看不見也填不了說明。

## 10:00 之後（他醒了）

- 貓草「一顆不夠」改「平淡」、0x「你來過六次」改「完全的平」重生上線（本機 CosyVoice；期間 GPU 被他的 whisper 佔著，排在後面跑）。
- **凌晨那批 107 句走 Larch 的有 18 句把（舞台指示）唸出來**——平台照板上的字唸，我踩回 9/14 修過的坑。
  用新工具 `tools/regen_larch_clean.py`（先讀線上版子存原樣→受影響那幾行換成 to_speech 的字→PUT→逐句生→PUT 原樣回去→對卡數）
  重生 22 句（18 句括號＋店員 2 句替身＋2 句連帶），0 失敗，板子換回原樣 809／936。20 句唸的字都對過，一句括號都沒有。
- `POST /voice/generate` 的 `emotion` 對輸出沒影響（同字同音色回同一個檔）。鐵塔「零失誤」現在是平台唸「靈失誤」的中性版，
  他說太兇的那個印象要再聽一次才知道有沒有解。走 Larch 的角色想換語氣只能換字或換音色。
- 更新說明在 `larch/RELEASES.md`；配音全部驗過之後已告訴他可以在網頁按發佈。

## 11:00～11:40：劇情模式錄滿五卷＋每卷一張 CG（都已上線）

- 路線再換七格（`87360fb`）：4|3 站牌空走→十四樓「站在大廳等」、8|0「問管理員那台螢幕」（不設變數）→便利商店「問店員每次都買一樣的那個客人」（兩顆蘿蔔 CG）、
  11|2／12|2／13|2 三次「翻到倒數第三頁」（不設變數）→保全「再站一次」「問他值幾點到幾點」「聽他講」（保全的手機 CG＋錄音）、
  12|0 第二次「站在看板底下」→頂樓「問諾亞那個穿西裝的」（諾亞錄音）、12|1 便利商店那格→工作室「問斑比她一個人住嗎」（斑比錄音）。
  三個錄音場加了「01開錄音機」的選擇。sim_rail 48 步全開、CG 來源卡 12/16（沒到的四張是結局與手機插播）。`walk` 已上線，**新開的局才吃到**。
- 五張 CG（`716ba81`）：諾亞、斑比、店員三張新生（Codex image-edit：立繪＋場景背景＋錄音機道具圖＋既有 CG 當畫風錨；主角照慣例只有雙手與黑筆記本），
  材料行老闆、保全兩張在原圖上加了錄音機。`larch/add_tape_cgs.py` 先讀線上再補：線上 809→812 卡、936→942 邊、收藏格 24→27。
  **push.py 整包推之後要重跑的腳本多一支：`larch/add_tape_cgs.py`（放在 add_relationship_cgs.py 之後）。**
- 自動玩家換 SwiftShader（軟體 GL）也一樣在第 6 天停住（跑到時限自然結束）——GL 不是原因，還是沒查出來。

## 12:00～13:00：五張錄音場 CG 重畫（他抓到四個問題）

- 問題：沒帶俯視圖（門、街、天空跑到對方背後）、錄音機五張各長各的、本子不是守則本、指甲有塗有沒塗。
- 做法收成 **`art/inv-cg/CG畫法.md`**：主角只畫兩隻手＋守則本；指甲短乾淨不塗（指甲油不會天天換，袖子可以換）；
  錄音機＝`art/items/item-recorder.png`、本子＝`art/items/item-rulebook.png`，每張都當參考圖傳；每個地點要有俯視平面圖
  （`layout-guides/`，新增 `roof-workbench`、`studio-desk`）；提示詞照 `rebuild-direction-manifest.json` 那種寫法把「對方背後只能有什麼」寫死；
  斑比工作室的牆＝滿牆格莉奇截圖；換圖要新檔名（媒體庫照檔名快取）。
- 既有 CG 也不一致（`bambi-wall-v3`、`lost-notebook` 紫指甲；`two-minutes`、`this-episode` 線圈筆記本），之後要回頭修，記在慣例檔一之三。
- 第三輪（他又抓到兩個）：提示詞寫「lying on the counter」四張變平躺、按鍵跑到側邊，只有諾亞自己立起來；材料行那張畫面下半是櫃台正面、道具像懸空。
  改成一律「STANDS UPRIGHT、按鍵在頂邊」，材料行明寫水平檯面＋影子。最終檔：`noah-v2`、`bambi-v4`、`clerk-v3`、`guard-v3`、`parts-v3`，
  `add_tape_cgs.py` 換上線（收藏格 27、卡數不變）。慣例檔補「一律立著」「道具要在看得見的水平檯面上」兩條。
- 第四輪：材料行的錄音機多畫一顆按鍵（一紅四黑）→ 只改那排重生成 `parts-v4`（提示詞要寫 EXACTLY FOUR）；
  斑比改到第四版整張出現波紋 → 他要求不要再改舊圖，從立繪＋俯視圖＋道具圖＋牆的參考重生全新一張（`bambi-v5`）。
  慣例檔補「四顆按鍵一紅三黑」「一張圖最多編輯兩次，超過就重生」。
- 第五輪：店員那張背景被畫成灰暗倉庫，他說要照本來場景圖（`bg-investigation/bg-store-night.png`，台灣 7-11 亮色調）。
  店員沒有立繪，人物拿 `cg-story-clerk-order` 當錨、背景拿場景圖當第五張參考，全新生成 `clerk-v4`。
  慣例檔補「背景要跟遊戲裡的場景圖同一間店」。最終檔：`noah-v2`、`bambi-v5`、`clerk-v4`、`guard-v3`、`parts-v4`。
- 自動玩家換 SwiftShader 一樣停在第 6 天，GL 不是原因。

## 13:00～14:00：旁白不講她不知道的名字；「這集有我」重畫

- 他問「玩家應該要知道他們的名字嗎？從旁白口中？」——對，旁白是她在看，全劇沒有人對她說過「諾亞」，
  「貓草」要到手辦店格莉奇那支片子喊出來才知道是那個客人。新檔 **`larch/inv/names.py`**：旁白正文
  諾亞→修收音機的、貓草→那個客人；筆記來源標籤諾亞：／貓草：→修收音機的：／客人：。保留格莉奇的「貓草！」與玩家問 ID 那句。
  build.py 的 `Board.add` 在掛配音前呼叫它（配音代號照字算）；patch_live.py 第七步對線上做同一件事。
  線上改 26 處、旁白重配 14 句（CosyVoice 本機）、換 15 個音檔網址，逐句核對 29 句代號 0 不合；卡數 812／942 不動。
- **`publish_voice.py --push --purge` 一起下會只清快取不複製**（`--purge` 先返回）。要分開：`--push` 自己會在 push 後清。
- 「這集有我」袖口抄了格莉奇、線圈筆記本 → 從舊圖當構圖錨＋守則本＋格莉奇立繪＋golden-hat 的手全新生成 `this-episode-v3`，
  用 `add_tape_cgs.py` 的 REPLACE 換上線（劇情 CG 的解鎖卡跟錄音帶那些同形，可以共用）。`add_story_cgs.py` 的檔名也改成 v3。
- 店員 tape CG 第五輪：背景照 `bg-store-night.png`（台灣 7-11 亮色調）重生 `clerk-v4`；慣例檔補「背景要跟場景圖同一間店」。
- build.py 那三行（import names、hide_names）跟別條線的改動混在同一個檔，只用 `git apply --cached` 挑那一段進 commit。

- 深夜背景掛到正篇：build.py `BG` 深夜欄寫了 `bg-apartment-hall`、`bg-noah-shop`、`bg-bambi-studio`（本機沒有這些檔，
  push 的查找落到正篇專案的圖），一樓／頂樓／斑比工作室深夜跟白天不是同一個地方，他在調查板與斑比對話抓到。
  改成調查篇自己的傍晚那張（`bg-lobby-evening`、`bg-roof-evening`、`bg-studio-evening`）；線上用 patch_live 第八步換了 6 處
  （3 張場景卡＋調查板 3 格）。要真的深夜版就另外生 `bg-<loc>-night` 再把表改回去。

## 14:00～15:00：清場卡、錄音帶 CG 只在錄音分支、刪除線改純文字記號、結局諾亞下台

- **刪除線**：U+0336 組合字元在他的電腦上印成方框（播放器逐字印純文字，chunk 裡沒有 line-through／innerHTML，抓 bundle 查過）。
  改成純文字記號 `〔劃掉：…〕`：build.py 筆記卡、patch_live `strike()`（連早先上線的 U+0336 一起換）、voice.to_speech 去記號照唸。
  設計稿 `橋段.md` 有兩處是直接寫組合字元的，改回 `~~`。9 句筆記重配。
- **清場卡**（他要的）：新模組 `larch/inv/clear_stage.py`，每個段落收尾的「回調查板」boardJump 前插一張沒有人的空卡
  （ghost 舞台、0.3 秒自動走），手機橫幅的觸發變數（open_studio、pr_request_sent）從前一張搬到清場卡上。
  push.py 在版面排版前呼叫；線上用 `larch/add_clear_stage.py` 先讀再補（冪等 `clear-<id>`）。選單的 `*-back` 不插。
  **push.py 整包推之後要重跑的腳本再多一支：`add_clear_stage.py`（放最後）。**
- **錄音帶 CG 漏洞**：三個錄音場「不開」那條也走回同一張收尾卡，解鎖卡掛在那裡 → 不錄也拿到 CG（他問到的）。
  `add_tape_cgs.py` 的 `source_card` 改成回傳取得錄音帶那張（grant-item），解鎖卡搬到「開錄音機」分支裡；
  `add_unlock` 改成把進出解鎖卡的邊全拆掉重接（不看邊的名字）。材料行、保全兩張從 REPLACE 改成 NEW（key 沿用 add_story_cgs 的
  `parts-ledger`、`guard-daughter`），保全那張從第四天女兒那段搬到「聽他講」的錄音分支。
- **結局諾亞站台**：inv-456「門幫我帶一下」之後四張（下樓、擦身、四次、出門）諾亞立繪一直在。設計稿 `橋段2.md` 那張卡補
  `**→ 下台 \`諾亞\`**`；線上 patch_live 第九步把 inv-457～460 清台。
- 他劇情模式只錄到 4 卷（少斑比）：判斷是那局開在 11:40 路線更新前（`walk` 開新局才寫進存檔）。`asked_斑比_黑洞` 只有那格自己會設，不是條件擋。

## 15:00～16:00：既有 CG 三張統一（他拍板「同一套生，不要指甲油、不要線圈筆記本」）

- `bambi-wall-v4`、`promise-v3`（指甲不塗）、`two-minutes-v2`（守則本攤開、手露出）：現圖當構圖錨＋golden-hat 的手＋守則本道具圖＋該角色立繪，
  提示詞「只改一件事」，三張一次過。`add_tape_cgs.py` REPLACE 換上線，`add_story_cgs.py` 檔名同步。慣例檔一之三改成「已統一」。
- 劇情 CG 現在全部合規：手不塗指甲、本子都是守則本、袖口不抄格莉奇。

## 16:00：白板重排（他要的：只動位置與群組，不接線）

- 先備份整個專案 `larch/inv/backups/inv-live-20260917-1542.json`（3.9 MB，重排前的原樣）。
- `larch/relayout_live.py`：跟 push.py 同一套版面（樞紐列／地點群組框／遊樂園框／開場列），推之前斷言邊與每張卡的 data 逐位元沒變，
  不過就不推；`--dry --snapshot` 只畫預覽 `backups/relayout-preview.png`。結果 922 卡（多 `grp-park` 框）、1057 邊一致。
  17 張沒進框的是沒有進邊的孤兒回板卡，排在群組上方一列。
- 自動玩家卡第 6 天的真因：它只按「01…」開頭的選項，店裡「開錄音機／不開」沒編號按不下去；劇情模式自動錄音邊把那張卡跳過了，重跑驗證中。

- **第 7 天深夜選單五個選項（他抓到）真因**：5|3 軌道要「跟靠窗那個人講話」＝鐵塔，他在店裡深夜是 0.25、保底第 2 次，
  第一次去多半不在 → 選單對不到全放行 → 玩家先跟貓草講 → `cat_visits` 提前 → 6|3、7|3 都對不到。
  修：board.html `whoIsThere()` 劇情模式在軌道目的地訪客一律在場（`forced`），patch_live 多兩段 swap 換到線上。自由探索不變。
  正在跑的那局救不回來（軌道已錯一步），要新開。

## 16:30～：劇情路徑頁（他要的：把自動玩家走的主線做成給不玩遊戲的人與 AI 看的頁）

- 產線：`MODE=story OUT=<dir> node tools/autoplay.mjs`（逐字稿 transcript.txt）→ `tools/path_from_transcript.py <transcript> --board <專案備份 json>`
  → `design/調查篇-劇情路徑.json`（天→時段→事件的樹：去哪、選什麼、板上便條、台詞、筆記、CG、錄音帶）→ `tools/gen_guide.py` 產
  `docs/guide/path.html` 與 `path.json` → `tools/gen_inv_art.py`（多轉收藏格用到的 CG，檔名清單取自最近一次備份）→
  `python3 ~/pwa-skill/tools/selfhost-font.py --family "Noto Serif TC" --weights 400,600 --out docs/fonts --chars-from docs/*.html docs/guide/*.html --url-prefix fonts/`
  （字型子集，新字不切會掉系統字型）→ `tools/update_sw.py`（SW 快取清單多了 guide/path）。
- 逐字稿的坑：打字機效果先印半句再印整句（同講者、後一行以前一行開頭→留後者）；CG 解鎖卡只印「新的調查篇紀念 CG 已加入收藏。」
  也會有半截，標題用「解鎖卡前一張對話的文字」對版子。筆記沒有標記，拿該步最後一段 ≥30 字的玩家獨白當筆記（偶爾會抓到接在後面的直播插播那句）。
- `gen_inv_art.py` 整批重轉會把既有 90 張圖重新編碼（位元不同），只加新 CG 進 commit，其餘 `git checkout` 回去。
- 自動玩家第二輪（貓草自動錄音那版）第 7 天深夜起選單全放行——就是那條錯邊；第三輪用修正後版子跑，資料以第三輪為準。

## 17:30：調查板換細紋軟木貼圖（他給了參考圖）

- 程式畫顆粒調了五輪都不像（薄片一疊就糊成斑塊），改用貼圖：Codex 生原創軟木紋 → 高通抹掉大尺度波紋 → 縮 0.25 → 平移半張接縫羽化 → 4×4 平鋪
  → `art/board-cork.webp`（1536×1024，566 KB），上傳到媒體庫。鏡像拼貼會出現萬花筒對稱紋，不能用。
- board.html：`CORK` 網址（push.py 用 `/*@@CORK@@*/` 灌 `art/board-cork.webp`；patch_live 寫死 `CORK_URL`）；有圖時 `.sky.tex` 用圖當底、
  canvas 只畫時段色溫（CSS multiply，`SKY_TEX` 白天近乎不染、晚上深夜壓暗）；沒圖退回原本深色顆粒。patch_live 多四段 swap，線上已換。
- 本機看板面：`scratchpad/boardshot.mjs`（開 board.html、postMessage larch:init）。

## 19:00～21:00：立繪差分十張（本機完成，**還沒套線上**）

- 依據：路線上台卡數＋劇本括號指示統計（貓草 35 卡／管理員 19 次指示／諾亞「沒有抬頭」×9…）。十張：
  貓草 away/face、管理員 report/stare、諾亞 down/up、店員 think/smile、斑比 away/look，都在 `art/inv-cast/sprite-<誰>-<姿勢>.png`，
  旁邊留原始底圖（`-on-green`／`-on-magenta`／`-raw-alpha`）。總表 `art/inv-cast/diff-candidates/總表-疊在場景上.png`、
  姿勢對台詞預覽 `預覽-姿勢對台詞.png`。
- **紫色系（貓草、諾亞、斑比）不能用洋紅幕**：生圖時顏色被帶偏（斑比桃粉變淡紫），去背時紫色被當半透明（內部半透明二三十萬）。
  改綠幕重生。本機 Codex 額度用完 → 走 .11 `codex-image-service`（`~/.config/codex-image/auth`，`/v1/images/generate` 帶 `reference_image_base64`）。
  它有兩張回的是透明 PNG（alpha 全 254、背景 687 塊碎片）：用 `scratchpad/rebuild_alpha.py` 的作法重建（門檻取主體、內部 255、只留兩像素邊帶）。
- 切換規則 **`larch/inv/poses.py`**：照卡上括號指示與旁白動作；一張卡是一整段對話，取卡裡**第一個**指示；同段落沿用、段落開頭回基本。
  push.py 在演員迴圈接 `pose_of`（整包重推也套）。線上工具 **`larch/apply_poses.py`**：先讀再改、**只改 `stage.actors[].url` 與 `characterLayers[].url`**，
  改前 `--backup`、改後逐張斷言 url 以外零改動、卡數邊數一致；`--restore 備份.json` 一鍵回滾。`--dry` 對線上（只讀）：102 張卡、10 種組合。
- `inv-cast` 裡 18:52 多出 `sprite-admin-reading / crossed-magenta / clerk-remembering / clerk-relaxed`（跟我的四張同內容、不同檔名），不是我放的，沒動。
- **上線步驟（等他點頭）**：1 `apply_poses.py --dry` 再看一次表 → 2 `--backup larch/inv/backups/inv-live-<時間>.json` 套上去
  → 3 對回讀卡數／邊數 → 4 `MODE=story node tools/autoplay.mjs` 跑一輪確認到謝幕、CG 與錄音帶數不變 → 5 網頁按更新。
  出事就 `--restore`。這一步不動邊、變數、文字、音檔，劇情走法不可能因此改變；風險只在圖片：某張卡換錯姿勢，看到再改規則重跑。

- **改成人審表制**（他問「難道不是該判斷劇情才換？」）：規則只產提案，`tools/pose_table.py --board <快照>` 產
  `design/調查篇-立繪姿勢.tsv`（卡、段落、誰、提案、觸發、審閱、決定、全文），`apply_poses.py` 與 push.py 都**讀表**（決定欄優先）。
  重產會保留已填的決定。共用卡（同一張卡被多個段落走到）取各段落提案裡最多的非基本姿勢，審閱欄標出。
  我審過 105 列、改了 36 處：「抬頭」不可吃到「沒有抬頭」；諾亞「上來了」回基本；「停了一下」在旁白裡常是玩家；店員「笑」只認本人
  （旁白「紙板人形…笑得標準」）；斑比看牆那幾張手動決定 base。最終 71 列、67 處要換，諾亞 up 目前 0 張用到。

- **21:10 立繪差分已套線上**（他說「不是 OK 的部份你推論就可以」）：備份 `larch/inv/backups/inv-live-20260917-2103.json`，
  `apply_poses.py` 換 67 處，回讀 922／1056、邊逐位元一致、67/67 網址核對。保險那段原本寫錯（迴圈裡重建比對物件）已修。
  Larch 上傳偶爾 504，重跑即可（沒 PUT 就沒改）。自動玩家正在跑確認；**市集要他按更新才會看到**。

## 工具

- `python3 larch/inv/patch_live.py [--dry]`：先讀線上再改再放回去。改講者名表、名字表、刪除線、軌道段落、音檔網址、謝幕副標。
- `MODE=story node tools/autoplay.mjs`：改門檻、接線、路線之後一定跑。現在會避開遊樂園（WebGL 無頭開不了）。
