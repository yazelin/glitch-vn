# 交接 2026-09-17 凌晨（他在 Meet 上對八個人直播試玩，邊玩邊修）

## 線上現在是什麼

- 調查篇專案 `project-d2fea918…`，主版子 809 卡／936 邊，樂園 26 張、CG 解鎖 13 張都在。**沒有整包重建過。**
- 講者名：鐵塔→經紀人、貓草→客人、諾亞→修收音機的（`push.py` 的 `DISPLAY`，全程固定）。
  貓草是 `@CatGrass_80`、諾亞是 `@Radio_Noah`，六個 ID 之二，卡片印本名等於印答案。
  **旁白正文還有 13 張寫「諾亞」、2 張寫「貓草」（手辦店那場），他還沒拍板要不要改。**
- 配音網址全部改走 jsDelivr（`novelkit.cdn()`）。urls.json 仍記 Pages 網址，掛卡時才換。
  改音檔後 `tools/publish_voice.py --push`，或手動 push 後 `--purge`，不然 @main 快取十二小時。
- 劇情模式軌道：目的地還沒解鎖時由軌道自己解鎖（board.html 的 railTo 那段），不再退成全開。
- 通關路線換回 9/12 的版本（`471435b`），`walk` 變數單獨 PUT 上線。
  Codex 改的那版留在 `design/調查篇-通關路線-codex改版-未採用.txt`。
- 筆記卡 `~~` 改組合字元刪除線；謝幕字卷副標改「十四天」。

## 全開的真因（已修，別再查一次）

Codex 改路線把第 2 天下午、第 3 天上午的便利商店換成一樓。店員「問店員一件事」要第三次以後才開
（`met_店員 >= 3`，第一天定稿 1358 行），第 3 天晚上那格變空，整條軌道往後滑：第 4 天晚上問不到
「這條街深夜還有什麼開著」→ `open_laundry` 沒設 → 第 5～8 天每晚軌道指洗衣店 → board.html railOff 全開
→ menu.html 對不到也放行 → 第 9 天工作室同理。修之前的版子用 `MODE=story` 自動玩家跑出來一模一樣。

## 沒做完的

1. **CG 排進劇情軌道**（他說「未來再弄更清楚、重新排整齊」）。靜態對照：9/12 路線上有 7 張劇情 CG
   （金色的帽子、斑比的牆、深夜的便利商店、兩分鐘＋第六次、我答應過他、她記得零件）；
   失物箱（幫他拿袋子）、保全的手機（保全四段：站在大廳等→再站一次→問他值幾點→聽他講）、
   兩顆蘿蔔（問店員每次都買一樣的那個客人）只有自由探索收得到。要排進去共需 6 格。
   Codex 那版是拿阿姨、南港站、手辦店、第 11～13 天晚上三次信箱去換前兩張——三次信箱疑似是目擊次數，
   動它會傷「四次」「第七行」。**排之前先弄清楚哪些格子是完美結局必需的。**
2. **自動玩家在修過的版子上一到 CG 解鎖卡就停住**（三輪都在第 6 天，沒印錯誤、沒進卡住偵測）。
   修之前的版子跑得完。沒查出來。人玩過得去。
3. 兩個檔案裡有這一輪的修正混著上一條線沒 commit 的改動，沒一起收：
   `larch/inv/build.py`（`~~`→U+0336）、`larch/inv/push.py`（DISPLAY＋推前讀線上對照，有外來卡就停；另一條線的手機通知順序 move_ops）。
   `larch/cards/board.html` 已收（2a2b101）。**push.py 沒 `--wipe-addons` 不會推**；真要整包推，推完照順序重跑
   `add_story_cgs.py → fix_park_cg_crossrun.py → add_relationship_cgs.py → replace_relationship_cg_art.py`。
4. 讀音：立著→豎著、單獨的錶→表、貓草「一顆不夠，三顆太多」改「煩但平」都已重配上線。
   「一顆不夠」那一版他還沒聽過。

## 04:00 之後補的（他回去睡了，交代「品質拉到最高、明天報告」）

- **正篇 87 個缺檔救回來了**：它們 8/22 就在 docs/voice，是上一條線清孤兒檔時在工作目錄刪掉、01:02 那次整批 commit 收進去的。
  從前一版 `git checkout` 還原（`f5ee…` 那批），不是重生。那四句短答也一起回來。
- **調查篇的諾亞、鐵塔、黑洞先生改用 Larch 的 MiniMax 音色逐句重生**（107 句；詼諧長者／廣播主持／紳士，跟正篇同一支）。
  `LARCH_VOICE` 登記了這三個人（gen_voice 從此不會在本機生他們）。`gen_larch_voice.py` 兩處修：線上講者是顯示名要換回本名
  才算得到代號；body 帶 `boardId`。**平台把最後一次 PUT 的版子當主線（activeBoardId），配音生成只在主線找卡**——
  patch_live 先推主版再推謝幕就把主線搶走了，全部 404；已把 activeBoardId 設回 board-main，patch_live 改成主版最後推。
  有 3 句含替身詞平台會照字唸（諾亞「。信箱」、鐵塔「唸」「零失誤」），要聽。
  **結果（05:00）**：107/107 生成、統一響度、發佈、推上去、快取清了（`49503e7`）。量過：諾亞中位 −19.0、鐵塔 −19.3、黑洞先生 −20.9 dB，
  只有鐵塔一句 −23.9 偏輕。第二輪重試時諾亞「……那是什麼。」平台逾時，留的是第一輪成功的那版。
- **遊樂園吃時段維持原設計**（05:30 作者確認；我一度改成不吃，已改回、線上也換回）。自動玩家不去遊樂園就好。
- **失物箱那張 CG 進了劇情軌道（05:40）**：第 2 天下午（原本只是去便利商店湊次數）→「信箱第三排那兩格」（trust_管理員 set 2）、
  第 9 天上午（站牌那場只設 `seen_aunt`，沒人再讀）→「電梯保養」（+1 → 3；**必須排在第 8 天「問管理員那台螢幕」之後，那格要 trust ≤ 2**）、
  第 10 天下午（南港站空走）→「幫他拿袋子」。sim_rail 對過：48 步全開、其他 CG 不掉、店員第三次仍在第 3 天晚上、櫃檯第六次仍在第 6 天下午。
  `walk` 已上線。commit `c838233` 的訊息說改了路線其實沒改（第 9 天那格被選單那行擋掉），真正的改在 `d7c26ed`。
- **建議先聽 6 句**已排進試聽頁（`試聽對照.html`）：一顆不夠、你來過六次、不是每天、諾亞「。信箱」、鐵塔「唸」「零失誤」。
  `listen.json` 是以代號為鍵的字典；`larch-labels.json` 要有 LARCH_VOICE 裡每個角色，不然 make_audition KeyError。
- **`tools/sim_rail.py`**：照劇情軌道走的變數模擬器（`--live <快照>` 把線上的 variableOps 蓋上來，管理員 trust 只在線上有）。
  現況：48 步全開得了（機率訪客在場時）、劇情 CG 來源卡走到 7/13；第七行／四次／這集有我／謝幕是插播與結局帶出來的，模擬不走那裡。
  機率訪客（5|3 店裡的鐵塔、10|2 一樓的黑洞先生）靠 board.html 的 pity 保底，模擬的 `--no-luck` 是最壞情況。
- **自動玩家在修過的版子上還是走不到底**：第七輪加了 60 秒沒動就截圖的看門狗也沒抓到——它是 6000 步跑滿正常結束，
  結束時畫面停在店裡「錄音機在包包裡。01 開錄音機／02 不開」那張選項卡。沒查出來。人玩過得去（他自己到第九天）。
- 響度：1440 句全量掃過，33 句短句另外用增益＋限幅拉齊（level_voice 的真峰值限制壓不到）；BGM 11 首差 2.7 dB、卡上一律 0.28。
- 線上驗證：1576 句 voiceUrl 全是 jsDelivr；線上代號對本機重建 0 句不同；抽 40 句下載比位元組 0 不同。
- 更新說明寫在 `larch/RELEASES.md`「調查篇 下一版」。**沒有按發佈**：remix 允許、活動標籤只有網頁看得到，agent API 發會看不見也填不了說明。

## 工具

- `python3 larch/inv/patch_live.py [--dry]`：先讀線上再改再放回去。改講者名表、名字表、刪除線、軌道段落、音檔網址、謝幕副標。
- `MODE=story node tools/autoplay.mjs`：改門檻、接線、路線之後一定跑。現在會避開遊樂園（WebGL 無頭開不了）。
