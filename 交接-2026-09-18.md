# 交接 2026-09-18（1.2 發佈之後的素材整理與切卡）

接續 `交接-2026-09-17.md`。1.2 已發佈（市集 release 3）。這天做的都是線上專案的素材與一張卡，沒有重建。

## 素材庫清理（他拍板「刪」）

- 清之前 527 筆，有引用 195 筆（44 MB）、沒引用 332 筆（105 MB）。判定方式：每筆的網址、檔名、id 逐一對活專案
  全文（去掉 media 那段）**與市集已發佈快照**（`GET /api/marketplace/<gameId>?play=1`，同樣去掉 media）兩邊，
  兩邊都沒出現才算沒引用。負控制：刪之前把「該留的」也列一遍，刪完對數量與逐筆 id。
- 端點 `DELETE /projects/:id/media` body `{"assetIds":[…]}`，50 筆一批。刪完 195 筆，卡 922／邊 1056 不動。
- 之後又補刪兩張故事 CG 第一版（下面第三節），最後 **202 筆、沒引用 0**。
- 刪掉的東西本機都有：舊 PNG／JPG 背景在 `art/bg-investigation/`、道具原圖在 `art/items/`、CG 舊版在 `art/inv-cg/`、
  Larch 試配的語音沒留（那 236 個是保全／店員／路人那批用 Larch AI 聲音試配的舊 take，正式版全走 jsDelivr 的本機克隆音）。
- 留著沒刪的：`sprite-noah-up.png`（現在 inv-331b 用到了）。

## 語音與圖片的實況（他問「真的都 jsDelivr？都 webp？」）

- 有聲台詞 1582 句：1479 句 jsDelivr、**103 句還在 Larch R2**（諾亞 65、經紀人 38，voiceMode=ai，MiniMax 詼諧長者／廣播主持）。
  這兩人沒有本機克隆音，那 102 個檔案是真的在用，沒動。旁白 199 句本來就無聲。
- 卡片資料裡每句只有 `voiceUrl`；卡層 `voiceMode` 只有 ai／shared／空三種，API 直接寫網址的語音在編輯器兩種標示都不亮，播放器照播。
- 有用到的圖 93 張全在 Larch R2；其中 44 張還是 PNG／JPG（31.6 MB）：立繪 22 張 24 MB（轉 webp q90 實測 2.1 MB→229 KB）、
  螢幕／道具 PNG 10 張 3.9 MB、場景 JPG 11 張 4 MB（含調查板 PHOTOS 的 7 張地點照）、謝幕背景 PNG 1.3 MB。
  **他拍板：圖先留在專案內、不改網址**，之後要把全部資源打包成市集素材（不含語音）；哪邊快晚點再測。
- 速度實測（同一個 2.1 MB 檔、交錯各 6 次）：R2 每次 1.39 s、沒有邊緣快取（r2.dev 是開發用網址，有流量限制）；
  jsDelivr 冷 2.1 s、暖 0.27 s。**他猜「Larch 進遊戲會先載好」不成立**：播放器 bundle（`Preview-*.js`）沒有預載任何專案素材，
  背景是到那張卡才 `new Image`，所以純看主機速度。
- 只在 jsDelivr、不在專案內的：語音 mp3 與手機直播影片（glitch-live repo）。cork／atlas／avatar 都已在 Larch。

## 專案內的圖 repo 一定要有（他說「我是原創作者」）

- 對過 93 張：89 張 repo 有，缺 4 張已補回並提交（commit 6c0a44f）：`art/park/bg-park-lobby-v1.webp`、
  `art/bg-investigation/bg-catgrass-home.jpg`（Larch 上那張「-corrected」與工作區這份逐位元相同，原本只是沒提交）、
  `art/inv-cg/cg-story-clerk-order.webp`、`art/inv-cg/cg-story-reception-sixth.webp`（第一版，線上已不用）。
- 兩張故事 CG 解鎖卡（店員點餐、櫃檯第六次）`cgOps` 原本同時解鎖第一版與 direction-v2，收藏格只列 v2；第一版那筆拿掉了，
  素材庫那兩張第一版跟著刪。

## 卡片上指到正篇專案的素材（角色工坊是空的）

- 這個專案 `characters` 是空的：立繪都是 `stage.actors[].url`＋`characterLayers[].url` 直接放圖，不是角色工坊的角色。
  沒有 CG 被當立繪；只是素材庫分類標錯（11 張樂園完成 CG 標成「場景」、5 張故事 CG 與貓草家背景沒分類），分類欄 API 改不到，只影響素材庫的分頁。
- 有 9 張圖直接指到正篇專案（斑比／貓草／鐵塔／0x／諾亞基本立繪、格莉奇／黑洞先生手機用、謝幕背景、空白立繪），素材打包抓不到、
  正篇清素材會斷。同一份檔已上傳到調查篇（API 上傳 572 KB 要 44 秒，共 9 分鐘），`larch/inv/swap_urls.py` 換了 776 處，兩版子回讀一致、舊網址殘留 0。
- **還指到正篇的只剩 6 首 BGM**（bgm-notebook／shop／cold／street／studio／living，本機 `art/bgm/`，各 9 MB 左右，共約 50 MB）。
  API 傳太慢，要搬的話從網頁「我的素材」上傳再用 `swap_urls.py` 換網址（對照表 JSON：舊網址→新網址）。

## inv-331 切卡讓諾亞抬頭（他拍板）

- 諾亞每張卡開場都是「沒有抬頭」「沒有停手」，規則全判 down，up 那張 0 卡用到。整段唯一抬頭處在 inv-331 中段：
  玩家講出「Radio Noah」，「修收音機的把手上的東西放下來。……那是什麼。」
- `larch/inv/split_331.py`（可重跑）：inv-331 留到「玩家：Radio Noah。」（6 句，down），新卡 `inv-331b` 從旁白那句起 9 句用 up，
  接回 inv-332（他「低下頭去」回 down）。同列 inv-332／clear-inv-333／inv-333 各往右一格。線 e304→inv-331b、新線 e304b。
  回讀 923／1057。姿勢表 `design/調查篇-立繪姿勢.tsv` 加了 inv-331b 那列（決定 up）。
- 沒做：自動玩家沒重跑（切卡不改分支）；`docs/guide` 的劇情路徑頁沒重產。build.py 若整包重建，切卡要重跑 `split_331.py`。

## 後續同一天：BGM 搬家、全部 webp（他拍板「留在專案內但全部 webp」）

- 6 首 BGM 他從網頁上傳（網頁傳的網址走帳號路徑 `…/larch/<時間戳>_bgm-x.mp3`，不是專案路徑，對名字不要對路徑），
  逐檔 md5 對 `art/bgm/` 相同後 `swap_urls.py` 換 34 處。**現在沒有任何網址指到正篇專案。**
- 44 張 PNG／JPG（＋諾亞 up）用 `tools/webp_live.py <快照>` 轉成 `art/live-webp/`（立繪 q90 保留透明、照片 q85；31.6 MB → 5 MB），
  他從資料夾全選上傳（漏了兩張，我用 API 補傳，每張約一分鐘）。對照表併進 `art/bg-investigation/webp/live-map.json`（70 筆），
  patch_live.py 讀那份，整包重建後跑它就會再換一次。
- 版子換 1228 處；**專案層（背包插件按鈕圖、三個道具變數預設值）走 `swap_urls.py --project`**：
  `PUT /projects/:id` 要包成 `{"project": 整包}`（原樣送會 400「project 必須是完整專案物件」），推後對卡／邊／素材數、版子逐位元。
- 舊檔 44 筆刪掉（過程兩次 502，重試成功，數量對得上）。**最終：素材 209 筆、沒引用 0、卡片引用的圖 102 張全 webp、素材庫沒有任何 png／jpg**；
  卡 923／邊 1057。
- 沒做：他沒在播放器裡看過 webp 立繪（透明邊）；發 1.3 前自己走一場看諾亞、斑比那幾張。

## 進板流暢化與手機直播（同一天再後來）

- 手機直播假載入 2.45 秒→1 秒（`STEPS`／`goLive`），本機 phone.html、glitch-live/card.html、線上六張卡（patch_live PHONE_PAIRS）三處同步。
- 實驗：sandbox iframe **有**瀏覽器快取（`scratchpad/cachetest.mjs`：開三次只抓一次），所以慢在第一次從 R2 拉軟木 566 KB＋拼版 258 KB（各 1.3 秒）與「圖到了整片換上」的閃。
- 板子改法（patch_live FADE_PAIRS／SMOOTH_PAIRS，board.html 同步）：整張 body 淡入 .45 秒；軟木貼圖另開 `#texImg` 層載到才淡入 .9 秒；
  拍立得 `.photo` 先黑（`::after` 黑蓋），拼版載到就退掉 1.1 秒（作者：載到就淡入，不要一張一張延時；曾套過延時版，STAG_PAIRS 負責換回）。
- 軟木與拼版網址改走 jsDelivr 釘 commit（`_CDN`），swap_board_js 末尾用 regex 把任何 `_board-cork／atlas.webp` 的 R2 網址換掉，整包重建也擋得住。
  Larch 素材庫那兩份留著（素材打包）。**每一組對都驗過套兩次不變**（`swap_board_js(t)==swap_board_js(swap_board_js(t))`），插入式的對要錨在前一組的最後一行，不然會插兩次。
- 無頭瀏覽器截圖驗過（`scratchpad/fadetest.mjs`）：60 毫秒暗底黑照片、900 毫秒軟木在、1.8 秒全到。
- **他發了 1.3（市集 release 4）**，那一版是「整板淡入＋貼圖各自淡入」，他看到「淡入了兩次」（不是重載兩次，是兩段動畫）。
  之後改成 ONE_PAIRS：`window.__reveal` 等貼圖 onload 或 0.6 秒（保險 1.5 秒）才加 `body.on`，貼圖來得及就掛 `.now` 不轉場，一次現身；已套線上，算 1.4。
  教訓：**同一段被連續改三版時，每一版都要留「上一版→這一版」的過渡對，而且要把最終版塞回原本那組的 new**，不然舊對會把中間版再插一次；
  驗法是拿三個起點（更舊的線上、現在的線上、本機）各套兩次，結果去掉網址後要逐字一樣。

## 工具

- `python3 tools/webp_live.py <GET /projects 快照>`：找卡片還在用的 PNG／JPG，轉 webp 到 `art/live-webp/`；`--upload` 可改走 API 上傳（慢）。
- `python3 larch/inv/swap_urls.py 對照表.json [--project] [--dry]`：換版子上的網址；`--project` 連 settings／variables 一起。
- `python3 larch/inv/split_331.py [--dry]`、`python3 larch/inv/swap_urls.py 對照表.json [--dry]`：都是讀線上→改→PUT，帶 If-Match、409 重讀重送，備份在 `larch/inv/backups/`。
- 素材庫稽核那段是 scratchpad 的一次性腳本，重點就是「對活專案全文＋市集快照，去掉 media 段」。
